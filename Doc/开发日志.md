# 开发日志

## 2026-06-05

### 阶段 7：多武器与武器升级

- 新增运行时武器状态列表，开局默认持有“追踪魔弹”。
- 原自动索敌投射物改为由“追踪魔弹”武器驱动，并随武器等级提升伤害、射程和冷却。
- 新增第二武器“旋刃”，解锁后会按独立冷却向周围环形齐射短程刀刃。
- 升级三选一加入武器相关选项：魔弹升级、解锁旋刃、旋刃升级。
- HUD 显示当前武器与等级，魔弹和旋刃投射物使用不同视觉颜色。
- 新增 `GameLogic.PlayModeTests`，覆盖武器选项解锁、武器升级和多武器同时发射。
- Unity MCP 已恢复可用，`editor/state` 显示 `ready_for_tools=true`，并可执行 `read_console`、`find_gameobjects`、`run_tests`。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留 2 个既有程序集引用版本冲突警告。
- Unity Play Mode：`GameLogic.Tests.RoguelikeStage7WeaponTests` 2 项通过，0 失败，控制台 0 错误。
- `git diff --check`：通过，仅输出工作区换行符提示。
- 阶段 7 状态更新为“已完成”，下一阶段进入阶段 8：被动道具与构筑。

## 2026-06-04

### 当前产品方向

- 项目方向明确为类《吸血鬼幸存者》的俯视角 2D 自动战斗肉鸽。
- 开发改为严格按阶段推进，当前阶段未完成 Unity 运行验收前不进入下一阶段。
- 新增 [类吸血鬼幸存者阶段开发路线.md](D:/Work/Unity_Project/TEngine_Game/Doc/类吸血鬼幸存者阶段开发路线.md:1)。

### 当前原型状态

- 已实现 WASD 移动、边缘刷怪、敌人追踪、接触伤害、范围攻击、掉落拾取、经验升级、随机三选一、难度成长、HUD、结算和永久金币。
- 已知偏差：移动未使用 `Rigidbody2D`；攻击尚未实现自动搜敌武器系统。
- 当前优先推进阶段 1：玩家移动正式化与 Unity Play Mode 验收。

### 项目规范

- 新增根目录 `AGENTS.md`。
- 功能实现前优先检查并复用 TEngine 和项目已有方案。
- 代码保持简洁、高效，不为未确认需求过度设计。
- 重要文档使用中文名称和中文内容。

### 文档清理

- 删除与当前自动战斗生存路线冲突的旧轻操作房间制排期和节点文档。
- 精简文档索引、系统总览、MVP 说明和开发日志，仅保留当前有效结论。

### 验证

- `dotnet build UnityProject.sln --no-restore`：通过，0 错误。
- `git diff --check`：通过。
- Unity 编辑器最近日志未发现新增脚本编译或空引用错误。

### 阶段 1：玩家移动正式化

- 玩家移动已从逻辑坐标直接累加改为 `Rigidbody2D.MovePosition` 驱动。
- 玩家运行时对象已挂载 `Rigidbody2D` 与 `CircleCollider2D`，并保留速度、暂停和地图边界规则。
- 新增主摄像机平滑跟随，修正运行时舞台重建时主摄像机可能被清理的问题。
- 重开一局时，玩家物理位置会同步回本局初始位置。
- Unity Play Mode 验收通过：移动状态与物理位置一致，暂停不移动，边界限制为 `x=7.50`，重开后回到原点，摄像机正常跟随。
- Unity 控制台 0 错误，阶段 1 完成，进入阶段 2。

### 阶段 2-3：敌人追踪与受伤验收

- 敌人运行时视图加入运动学 `Rigidbody2D`、触发碰撞体和物理帧位置同步。
- Play Mode 中敌人数量从 2 增至 3，首个敌人与玩家距离从 `4.99` 降至 `3.40`。
- 接触伤害验收将玩家生命从 `100` 降至 `92`，生命与受伤链路正常。
- Unity 控制台 0 错误，阶段 2 与阶段 3 完成，进入阶段 4。

### 阶段 4：自动攻击武器

- 将方向范围攻击替换为最近敌人自动索敌和直线投射物。
- 投射物包含速度、伤害、剩余射程，并有独立可见视图。
- Play Mode 中目标生命从 `23` 降至 `11`，随后被投射物击杀，击杀数变为 `1`。
- Unity 控制台 0 错误，阶段 4 完成，进入阶段 5 与阶段 6 验收。

### 阶段 5-6：升级与难度曲线验收

- 升级后进入 `RewardChoice`，局内时间冻结，并生成 3 个不同强化选项。
- 选择强化后恢复运行，本次验证移动速度从 `4.50` 提升到 `4.95`。
- `0.4` 秒时敌人生命、攻击、速度为 `23/9/1.43`；`120.4` 秒后为 `43/17/1.75`，单次刷怪数从 1 增至 3。
- Unity 控制台 0 错误，阶段 5 与阶段 6 完成。

### 阶段 10/12：UI 与完整流程验收

- HUD、升级三选一和结算界面完成截图复验。
- 修复小尺寸按钮标签高度不足导致文字被裁切的问题，结算重开按钮已正常显示。
- 操作提示更新为“武器自动攻击最近敌人”，移除过期的方向攻击说明。
- 死亡后进入 `Defeated`；重开后恢复 `Running`、生命 `100`、等级 `1`。
- Unity 控制台 0 错误，阶段 10 与阶段 12 完成。
