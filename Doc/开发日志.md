# 开发日志

## 说明

本文件用于记录每次重要开发推进后的摘要，按时间顺序持续追加。

记录原则：

1. 每次对话结束后，如果产生了重要决策、代码变更、运行验证结果、排期调整或问题修复，均在本文件追加一条日志。
2. 日志写事实，不写空泛表述。
3. 如果某次内容已经扩展成独立文档，本文件保留简要结论和文档索引。

---

## 2026-05-24

### 产品方向确认

- 确认项目方向为轻操作肉鸽
- 确认内容和规则逐步迁移到配置
- 确认尽快进入正式 UI 结构，不继续堆临时 HUD

### 排期确认

- 内部目标完工时间定为 `2027-01-15`
- 已形成节点级排期和阶段任务拆解

### 原型开发进展

- 已完成肉鸽原型单局流程基础闭环
- 已接入战斗、营火、宝箱、商店、Boss 等基础房间逻辑
- 已完成商店价格、金币校验和离开商店逻辑
- 已将营火、宝箱、通用奖励池拆分

### 文案与运行验证

- 肉鸽相关用户可见文案已统一为中文
- Unity Play Mode 中修复了内置字体问题：`Arial.ttf` 改为 `LegacyRuntime.ttf`
- 当前游戏代码可跑，剩余主要报错来自 `MCP-FOR-UNITY` 连接流，不属于游戏逻辑报错

### 文档约定

- 重要结论统一整理到 `Doc`
- 排期与节点纪要使用独立文档维护
- 后续每次重要更新同时追加本文件

### 文档新增

- 新增 [框架使用指南.md](D:/Work/Unity_Project/TEngine_Game/Doc/框架使用指南.md:1)
- 新增 [文档索引.md](D:/Work/Unity_Project/TEngine_Game/Doc/文档索引.md:1) 作为 `Doc` 入口

### 快速开发推进

- 按 [肉鸽开发排期及节点纪要.md](D:/Work/Unity_Project/TEngine_Game/Doc/肉鸽开发排期及节点纪要.md:1) 中“技术方案冻结”方向，先拆出肉鸽原型的内容池和文案边界
- 新增 `RoguelikeChoiceCatalog`，集中管理奖励、营火、宝箱、商店选项池，为后续迁移到 Luban 配置预留入口
- 新增 `RoguelikeText`，统一房间名、阶段名和奖励提示文案，移除 `RoguelikeGame`、`RoguelikeRunService`、`BattleMainUI` 中的重复房间名逻辑
- `dotnet build UnityProject.sln --no-restore` 验证通过；剩余为既有 Unity/MCP 引用警告，无新增编译错误
