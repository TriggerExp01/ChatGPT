# 开发日志

## 2026-06-07

### 阶段 36：配置校验与生成流程加固

- 新增 `RoguelikeConfigValidator`，对敌人、武器、奖励选项、被动遗物和刷怪阶段做最小完整性校验，覆盖空表、空 ID、重复 ID、关键字段非法、效果为空、空奖励池、刷怪敌人引用和权重数量不一致等问题。
- 校验器支持可选资源地址检查，当前必要资源包含五类音效、三类特效 Prefab 和四个敌人/投射物 Actor Prefab，继续按 `AddressByFileName` 与 `Assets/AssetRaw` 收集目录验证。
- `RoguelikeGame` 首次拿到 Luban `Tables` 后会执行一次配置校验，缓存 `ConfigValidationIssues`；若发现问题，通过 TEngine `Log.Error` 输出中文摘要，让表驱动错误尽早暴露，不等运行时空引用。
- 新增 `RoguelikeStage36ConfigValidationTests`，覆盖真实配置 bytes 零问题、必要资源存在、缺失资源地址提示和运行时校验结果缓存。
- `Doc/框架使用指南.md` 已补充 Luban 生成后验证说明，后续修改 Excel、生成 bytes 或接入新资源后应同步跑配置校验。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 可读取且 `ready_for_tools=true`，`read_console` 可执行；阶段 36 单测 `RoguelikeStage36ConfigValidationTests` 4/4 通过。
- Unity Play Mode：全量 `GameLogic.PlayModeTests` 76/76 通过；测试后出现一次 Unity Test Framework `PlaymodeLauncher.cs:113` 退出噪声并留下临时 `InitTestScene`，已清理临时文件和控制台，最终控制台 0 Error。

### 阶段 35：敌人和投射物 Prefab 化第一轮

- 新增 `Assets/AssetRaw/Actor` 下的四个轻量表现 Prefab：`Roguelike_Enemy_Common.prefab`、`Roguelike_Enemy_Boss.prefab`、`Roguelike_Projectile_MagicBolt.prefab`、`Roguelike_Projectile_PiercingDart.prefab`，继续复用现有白色像素 Sprite 和 `AddressByFileName` 收集规则。
- `RoguelikeBattleStageView` 新增普通敌人、Boss、魔弹和穿透飞镖 Prefab 地址常量，通过 TEngine `GameModule.Resource` 做同步加载尝试；资源模块不可用、地址缺失或非本轮投射物类型时自动回退到运行时 Sprite 表现。
- 敌人和投射物视图继续复用原有舞台临时视图池，池化复用后会重新同步根节点 SpriteRenderer、位置、旋转、体型、颜色、Boss 差异和受击闪白，不改启动、热更、YooAsset 收集配置或资源基础流程。
- 新增 `RoguelikeStage35PrefabPresentationTests`，覆盖 Actor 收集目录资源存在、缺失 Prefab 地址时 Sprite 回退、Boss/普通敌人表现差异、穿透飞镖旋转与尺寸同步、敌人和投射物视图池复用。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 可读取且 `ready_for_tools=true`，`read_console` 可执行；阶段 35 单测 `RoguelikeStage35PrefabPresentationTests` 3/3 通过。
- Unity Play Mode：全量 `GameLogic.PlayModeTests` 72/72 通过；测试后出现一次 Unity Test Framework `PlaymodeLauncher.cs:113` 退出噪声，已清理控制台，最终控制台 0 Error。

### 阶段 34：命中特效与拾取特效 Prefab 接入

- 新增 `Assets/AssetRaw/Effects` 下的自制白色像素 Sprite 与三类半正式轻量 Prefab：`Roguelike_HitEffect.prefab`、`Roguelike_KillEffect.prefab`、`Roguelike_PickupEffect.prefab`。
- `RoguelikeGame` 新增 `RoguelikeEffectCue` 运行时事件队列，在命中、击杀和拾取节点分别发出表现 cue，避免舞台视图依赖对象删除猜测特效时机。
- `RoguelikeBattleStageView` 新增特效 Prefab 地址常量、TEngine `GameModule.Resource` 同步加载尝试、短生命周期淡出组件和回收池；资源模块不可用或地址缺失时自动回退到运行时 Sprite 表现。
- 特效视图回收时保留初始颜色，避免淡出后的透明度污染下一次复用。
- 新增 `RoguelikeStage34EffectPrefabTests`，覆盖特效资源存在、命中/击杀/拾取 cue 生成、特效视图生成回收和缺失资源回退。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`read_console` 可执行，测试前后均可清理并读取控制台；一次全量 Play Mode 曾卡在 Unity Test Runner 初始化，已按既定流程停止 Play Mode、重载 `Assets/Scenes/main.unity` 并清理控制台后重跑。
- Unity Play Mode：全量 `GameLogic.PlayModeTests` 69 项通过，0 失败；测试后出现一次 Unity Test Framework `PlaymodeLauncher.cs:113` 退出噪声，已清理，最终控制台 0 Error。

### 阶段 33：正式音效替换与混音规则

- 新增原创短音效 `Roguelike_Boss.wav` 和 `Roguelike_UIConfirm.wav`，补齐命中、拾取、升级、Boss 和 UI 确认五类音效地址。
- `RoguelikeGame` 将音效路径、音量和短冷却整理为统一常量，继续通过 `GameModule.Audio.Play(TEngine.AudioType.Sound, path, false, volume, true)` 播放。
- 音效冷却从单一全局冷却改为按音效地址独立冷却，避免密集命中吞掉拾取、升级、Boss 或 UI 确认音。
- Boss 出场会触发 Boss 音效；升级继续触发升级音效；奖励卡、暂停/继续、重开和结算重开接入 UI 确认音。
- 新增 `RoguelikeStage33AudioMixTests`，覆盖五类音效资源存在、音量/冷却范围、UI 确认音冷却衰减和 Boss/UI 音效互不阻塞。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 可读取且 `ready_for_tools=true`，`read_console` 可执行；单类 Play Mode 过滤曾两次卡在 Unity Test Runner 初始化，已按既定流程停止 Play Mode、重载 `Assets/Scenes/main.unity` 并清理控制台。
- Unity Play Mode：全量 `GameLogic.PlayModeTests` 66 项通过，0 失败；测试后清理 Unity Test Runner 临时场景和 ProjectSettings 噪声，最终控制台 0 Error。

### 阶段 32：结算与局外成长最小实现

- 继续复用现有 `PlayerPrefs` 永久金币链路，没有新增账号、背包或复杂存档系统。
- `RoguelikeGame` 增加永久攻击加成、永久金币进度和下一点攻击所需金币等只读属性，`StartNewRun` 继续按每 100 永久金币提供下局初始攻击 +1。
- `SettlementSummary` 区分胜利“星辉凯旋”和失败“旅途暂歇”，并展示本局数据、本局金币、永久金币和局外成长进度。
- `BattleSettlementUI` 从单段文本升级为结算成长面板，包含角色剪影区、胜败标题、战斗数据、收益区、永久成长进度条、成长说明和重新开始入口。
- 新增 `RoguelikeStage32SettlementGrowthTests`，覆盖结算金币只入账一次、重开后永久攻击加成生效、结算 UI 展示胜利/数据/收益/成长进度并能重新开始。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 可读取且 `ready_for_tools=true`，`read_console` 可执行；阶段 32 单类 Play Mode 测试成功返回。
- Unity Play Mode：`GameLogic.PlayModeTests` 62 项通过，0 失败；测试后清理 Unity Test Runner 临时场景和 ProjectSettings 噪声，最终控制台 0 Error。

### 阶段 31：升级三选一卡牌体验打磨

- 将 `BattleChoiceUI` 从 3 个基础按钮升级为三张奖励卡牌，每张卡包含图标占位、标题、效果描述、价格和状态提示。
- 奖励选择面板加入星辉/月辉装饰块与更高的卡牌区域，强化二次元轻幻想 Bullet Heaven 的奖励展示方向，同时继续复用当前 `UIWindow` 体系。
- 金币不足时付费卡会显示“金币不足”并保持不可点击；金币足够时点击卡牌仍走 `RoguelikeGame.ChooseReward`，扣费、应用效果并恢复 `Running`。
- `RoguelikeSurvivalWeapon` 增加当前阶段 5 级上限，满级武器升级项不再进入奖励池，避免卡牌池出现无效升级。
- 新增 `RoguelikeStage31RewardCardTests`，覆盖卡牌结构、价格/不可选状态、选择后恢复运行和满级武器过滤。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 可读取，`read_console` 可执行；单类 Play Mode 测试 `GameLogic.Tests.RoguelikeStage31RewardCardTests` 已启动并成功返回。
- Unity Play Mode：`GameLogic.PlayModeTests` 59 项通过，0 失败；最终控制台 0 Error。

### 阶段 30：HUD 信息密度与二次元轻幻想表现打磨

- 将 `BattleHudUI` 从单条顶部信息栏调整为四块低遮挡布局：左上角色状态、中上时间压力、右上构筑槽、底部战斗提示。
- 角色状态面板展示头像占位、生命条、经验条、等级、生命、攻击、局内金币和永久金币，让玩家优先读到生存状态。
- 时间压力面板集中显示生存时间、敌人数量和击杀数；构筑槽面板提供武器/被动图标槽与构筑摘要，强化二次元轻幻想 Bullet Heaven 的构筑展示方向。
- 底部提示面板改为低高度居中显示，仅保留当前战斗提示，减少对战斗主体的遮挡。
- `RoguelikeUIFactory` 新增轻量 `CreateImage` 封装，当前阶段仅用于运行时 UI 占位图块，不引入外部资源或第二套 UI 框架。
- 新增 `RoguelikeStage30HudPolishTests`，覆盖 HUD 分区锚点、角色/压力/构筑文本刷新和武器/被动图标槽生成。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 可读取，`read_console` 可执行；单类 Play Mode 测试 `GameLogic.Tests.RoguelikeStage30HudPolishTests` 3/3 通过。
- Unity Play Mode：`GameLogic.PlayModeTests` 55 项通过，0 失败；最终控制台 0 Error。

### 阶段 29：地图空间体验第一轮

- 在固定场地中新增 3 个轻量 `RoguelikeArenaObstacle`，形成简单绕行空间，但不引入随机地图、寻路系统或复杂地图生成。
- 玩家 `SyncPlayerPosition` 与运行时移动会通过统一障碍解析被推出障碍，继续保留原有场地边界夹取。
- 敌人追踪移动接入相同障碍阻挡，投射物命中障碍后会立即回收，避免远程攻击无视地图空间。
- `RoguelikeBattleStageView` 绘制障碍块与高光表现，让当前运行时生成地图从纯地砖变为有阻挡层次的固定场地。
- 新增 `RoguelikeStage29MapSpaceTests`，覆盖玩家/敌人不会停在障碍内、投射物命中障碍会移除、舞台障碍视图数量与尺寸正确。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 可读取，`read_console` 可执行；Play Mode 测试运行器仍偶发初始化卡住，已通过停止 Play Mode、重载 `Assets/Scenes/main.unity`、清理 MCP 测试 job 后恢复。
- Unity Play Mode：`GameLogic.PlayModeTests` 52 项通过，0 失败；最终控制台 0 Error。

### 阶段 28：Boss 战第一轮可玩化

- 在 Boss 生成时写入“地牢之心出现，击败它完成本局目标。”，并触发更强相机震动，让阶段 22 的 600 秒 Boss 节点从隐藏目标变成明确战斗事件。
- `RoguelikeBattleStageView` 根据 `RoguelikeSurvivalEnemy.IsBoss` 区分 Boss 表现：Boss 使用更大的紫色体型，普通敌人仍使用原红色体型与受击闪白。
- Boss 被击败后额外获得 25 金币，再进入 `Victory` 并执行一次性永久金币结算，阶段 27 的胜利目标继续保留。
- 新增 `RoguelikeStage28BossPlayableTests`，覆盖 Boss 出场身份与提示、Boss 视图颜色/体型差异、Boss 击败奖励和胜利结算。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 可读取，`read_console` 可执行，Play Mode 测试运行器曾出现一次初始化卡住，已通过停止 Play Mode、重载 `Assets/Scenes/main.unity`、清理 MCP 测试 job 后恢复。
- Unity Play Mode：`GameLogic.PlayModeTests` 49 项通过，0 失败；最终控制台 0 Error。

### 阶段 27：局内目标与胜利条件扩展

- 固化“击败地牢之心”作为当前单局最小胜利目标，继续复用阶段 22 的 Boss 节点，不新增复杂 Boss 技能或多阶段框架。
- `RoguelikeSurvivalEnemy` 新增 `ConfigId` 与 `IsBoss` 标记，配置表生成的 Boss 敌人会在运行时保留身份。
- Boss 被击败时进入 `Victory`，停止当帧后续战斗更新，写入“击败地牢之心，生存目标达成。”并触发结算入账。
- 普通敌人击杀仍只增加击杀数并生成经验/金币掉落，不会误触胜利。
- 新增 `RoguelikeStage27VictoryConditionTests`，覆盖 Boss 击杀胜利、普通敌人击杀不胜利、胜利后重开恢复 Running 与局内状态归零。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 显示 `ready_for_tools=true`，刷新导入后控制台 0 错误。
- Unity Play Mode：`GameLogic.PlayModeTests` 46 项通过，0 失败。

### 阶段 26：掉落与金币用途最小闭环

- 复用现有 `RoguelikeRunState.Gold`、`TrySpendGold`、`RoguelikeChoiceOption.Cost` 和 `CanAfford`，没有新增商店系统或复杂经济系统。
- `ChooseReward` 接入付费选择规则：金币不足时保留三选一界面并提示“金币不足”，金币足够时先扣费再应用选项效果。
- 升级三选一新增付费选项“战地补给”，花费 8 金币立即恢复 35 生命，让局内金币有最小消费口径。
- 保留敌人击杀概率掉落金币与结算入账永久金币链路，未消费的局内金币在胜利或失败结算时只入账一次。
- 新增 `RoguelikeStage26GoldLoopTests`，覆盖金币拾取入账、付费选项扣费、余额不足不生效且不关闭选择、结算永久金币只入账一次。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 显示 `ready_for_tools=true`，刷新导入后控制台 0 错误。
- Unity Play Mode：`GameLogic.PlayModeTests` 43 项通过，0 失败。

### 阶段 25：被动道具第二轮扩展

- `roguelike_relic.xlsx` 新增三个只影响现有数值的被动：`vital_sigil`“生息符文”、`eagle_eye`“鹰眼纹章”、`starlight_soles`“星辉轻靴”。
- “生息符文”提供最大生命 +18 并恢复 18 生命；“鹰眼纹章”提供攻击 +2 与暴击 +6%；“星辉轻靴”提供移动速度 +0.2 与拾取吸附范围 +0.4。
- 本阶段优先复用现有 `EEffectType` 与被动解释层，没有新增冷却缩减或经验获取效果，避免提前改动武器冷却结算与经验倍率链路。
- 重新执行 `gen_code_bin_to_project.bat`，更新 `roguelike_tbroguelikerelic.bytes`；生成 C# 仅有空白变化，已恢复。
- 新增 `RoguelikeStage25PassiveExpansionTests`，覆盖 Luban bytes 读取、新被动进入选择池、属性实际生效、重复叠加与 HUD 被动汇总显示。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 显示 `ready_for_tools=true`，刷新导入后控制台 0 错误。
- Unity Play Mode：`GameLogic.PlayModeTests` 39 项通过，0 失败。

### 阶段 24：第四武器最小实现

- 新增第四武器“星环脉冲”，定位为短程环形脉冲武器，按冷却向周围释放多道短程投射物。
- 扩展 `RoguelikeWeaponType.StarRingPulse`，复用现有 `RoguelikeSurvivalProjectile`、投射物更新、对象池和舞台视图同步逻辑。
- `roguelike_weapon.xlsx` 新增 `star_ring_pulse`：基础伤害 7、每级伤害成长 3、基础间隔 0.95、基础射程 2.4、投射物速度 6.2。
- 升级三选一会提供“解锁星环脉冲”和后续升级选项，HUD 武器汇总可显示星环脉冲等级。
- 舞台视图为星环脉冲提供独立的绿色圆形投射物表现，仍使用运行时 Sprite，不引入新资源框架。
- 新增 `RoguelikeStage24FourthWeaponTests`，覆盖配置表、解锁/升级、短程环形发射和四武器同局发射。
- 重新执行 `gen_code_bin_to_project.bat`，更新 `roguelike_tbroguelikeweapon.bytes`；生成 C# 仅有空白变化，已恢复。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 显示 `ready_for_tools=true`，刷新导入后控制台 0 错误。
- Unity Play Mode：`GameLogic.PlayModeTests` 35 项通过，0 失败。

### 阶段 23：武器配置与数值平衡第一轮

- 通过 `roguelike_weapon.xlsx` 微调三把现有武器，不新增武器系统、稀有度、进化或第二套投射物框架。
- 追踪魔弹定位为稳定单体输出：基础伤害 13、每级伤害成长 4、基础间隔 0.52、基础射程 5.8、投射物速度 9.5。
- 旋刃定位为近身群体输出：基础伤害 8、每级伤害成长 3、基础间隔 0.78、基础射程 3.0、投射物速度 7.2。
- 穿透飞镖定位为远程穿透输出：基础伤害 11、每级伤害成长 4、基础间隔 0.82、基础射程 7.8、投射物速度 10.5。
- 新增 `RoguelikeStage23WeaponBalanceTests`，覆盖武器表数值、运行时解释结果、升级收益差异和三武器同局发射。
- 重新执行 `gen_code_bin_to_project.bat`，更新 `roguelike_tbroguelikeweapon.bytes`；生成 C# 仅有空白变化，已恢复。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过。
- Unity MCP：`editor/state` 显示 `ready_for_tools=true`，刷新导入后控制台 0 错误。
- Unity Play Mode：`GameLogic.PlayModeTests` 31 项通过，0 失败。

### 阶段 22：刷怪节奏与时间目标细化

- 将 `roguelike_spawn_stage.xlsx` 从 3 段扩展为 5 段节奏：0 秒、60 秒、180 秒、360 秒、540 秒。
- 普通刷怪数量从 1 逐步提升到 5，并按阶段调整黏液怪、洞窟蝙蝠、星尘游灵、遗迹守卫、晶壳虫和蘑菇守卫的权重。
- 精英敌人“月影骑士”从 120 秒开始进入节奏，后期 540 秒持续进入压力段。
- Boss 敌人“地牢之心”固定到 600 秒节点，运行时增加一次性 Boss 刷出锁，避免到点后重复随机刷 Boss。
- 新增 `RoguelikeStage22SpawnPacingTests`，覆盖 10 分钟节奏表、运行时阶段切换、Boss 节点一次性刷出、胜利/失败/重开流程稳定性。
- 重新执行 `gen_code_bin_to_project.bat`，更新 `roguelike_tbroguelikespawnstage.bytes`；生成 C# 仅有空白变化，已恢复。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 显示 `ready_for_tools=true`，刷新导入后控制台 0 错误。
- Unity Play Mode：`GameLogic.PlayModeTests` 27 项通过，0 失败。

### 产品 UI 与资源策略固化

- 后续重要且会影响长期开发的决定必须先与用户确认，再固化到现有有效文档中；未经确认不直接落成代码、资源导入、Unity 验证或提交。
- UI 商业化方向确定为“二次元轻幻想 Bullet Heaven UI”：强调角色感、卡牌奖励、图标化构筑、低遮挡战斗 HUD 和 Steam PC 玩家可读性。
- UI 不走暗黑硬核，也避免廉价手游式满屏按钮、红点、礼包入口和遮挡战斗主体的大面板。
- 资源策略确定为“免费可商用资源 + GPT 绘画”：免费资源用于字体、基础 UI、占位图标和简单特效；GPT 绘画用于角色头像、奖励卡牌、武器/被动图标、掉落物和 Steam 展示图。
- 所有外部免费资源必须记录来源、作者、许可和用途；优先使用 CC0 或明确可商用资源，CC-BY 资源必须保留署名信息。
- 阶段 30-32 和阶段 40 已更新为对应 UI 表现、卡牌奖励、结算展示和 Steam 截图验收目标。

### 阶段 21：敌人与战斗内容第二轮扩展

- 按确认后的阶段 21 方案撤回此前未确认的 5 个 UI 脚本改动，保留已确认的文档固化内容，避免阶段 21 提交混入 UI 打磨。
- 在 `roguelike_enemy.xlsx` 中新增 3 个普通敌人：星尘游灵、蘑菇守卫、晶壳虫。
- 在 `roguelike_enemy.xlsx` 中新增 1 个精英敌人：月影骑士。
- 更新 `roguelike_spawn_stage.xlsx`：星尘游灵进入阶段 1-2 普通刷怪池，蘑菇守卫进入阶段 2-3 普通刷怪池，晶壳虫进入阶段 3 普通刷怪池，月影骑士进入阶段 2-3 精英敌人配置。
- 重新执行 `gen_code_bin_to_project.bat`，更新 `roguelike_tbroguelikeenemy.bytes` 和 `roguelike_tbroguelikespawnstage.bytes`。
- 新增 `RoguelikeStage21EnemyExpansionTests`，覆盖新增敌人配置读取、刷怪阶段引用、内容目录构建和运行时新增普通敌人刷出。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有 AdditionalFile、nullable 和程序集引用版本冲突警告。
- `git diff --check`：通过，仅输出工作区换行符提示。
- Unity MCP：`editor/state` 显示 `ready_for_tools=true`，刷新导入后 `external_changes_dirty=false`。
- Unity Play Mode：`GameLogic.PlayModeTests` 23 项通过，0 失败，控制台 0 错误。

### 阶段 21-40：下一轮实施路线固化

- 在 `Doc/类吸血鬼幸存者阶段开发路线.md` 中正式固化阶段 21-40 的后续操作列表，作为阶段 20 之后的唯一推进依据。
- 下一轮路线继续保持阶段制：每次只推进一个阶段，当前阶段完成 Unity 运行验收后再进入下一阶段。
- 阶段 21-40 的主线顺序为：敌人与战斗内容扩展、刷怪节奏细化、武器平衡、第四武器、被动扩展、金币用途、胜利条件、Boss 战、地图空间、HUD、升级体验、局外成长、正式音效、特效 Prefab、敌人与投射物 Prefab、配置校验、性能基线、对象池压测、新手提示和版本验收。
- 后续阶段仍优先复用 TEngine、`GameModule`、现有 UI 模块、Luban 配置表、TEngine 资源模块和音频模块，不引入 Addressables、第二套配置系统、复杂技能树、稀有度或武器进化系统。
- 阶段 21 明确为“敌人与战斗内容第二轮扩展”：先新增一批差异化普通敌人和精英敌人，继续走 Luban 配置与当前刷怪解释链路。

## 2026-06-05

### 阶段 16-20：后续执行阶段固化

- 正式将后续 5 个执行阶段纳入路线：阶段 16 配置表清理与表结构补齐、阶段 17 敌人与刷怪节奏配置化扩展、阶段 18 第三武器最小实现、阶段 19 被动效果配置解释层、阶段 20 资源音效与命中反馈最小接入。
- 阶段 16 优先解决 Luban 表中文文本旧编码痕迹，并补齐现有武器、被动和刷怪阶段的最小配置结构，避免继续把内容堆在 `RoguelikeGame`。
- 阶段 17-20 均要求保留当前核心闭环和 TEngine 模块边界，不引入 Addressables、第二套配置系统、复杂技能树、稀有度或武器进化系统。
- 每个阶段完成后仍需执行 `dotnet build UnityProject\UnityProject.sln --no-restore`、`git diff --check` 和 Unity Play Mode 验收。

### 阶段 16：配置表清理与表结构补齐

- 修复 `roguelike_enemy.xlsx`、`roguelike_choice.xlsx` 和 `roguelike_relic.xlsx` 中的中文文本旧编码痕迹。
- 新增 `roguelike_weapon.xlsx` 和 `roguelike_spawn_stage.xlsx`，并注册到 `__tables__.xlsx`。
- 重新生成 Luban C# 类与 bytes，`Tables` 已包含 `TbRoguelikeWeapon` 和 `TbRoguelikeSpawnStage`。

### 阶段 17：敌人与刷怪节奏配置化扩展

- `RoguelikeGame` 新增刷怪阶段读取逻辑，按 `start_time` 选择当前阶段。
- 普通敌人 ID、权重、刷怪数量、精英敌人节点、Boss 节点和生命成长从配置表读取。
- 保留原有阈值刷怪回退，避免配置不可用时阻塞核心闭环。

### 阶段 18：第三武器最小实现

- 新增第三武器“穿透飞镖”，接入升级三选一和武器汇总显示。
- 飞镖复用当前投射物与对象池，但可记录已命中敌人，支持一次飞行命中多个敌人。
- 舞台视图为穿透飞镖提供独立颜色和形状，不新增复杂特效框架。

### 阶段 19：被动效果配置解释层

- 扩展 Luban `EEffectType`：`AddPickupRadius`、`AddProjectileDamageMultiplier`、`AddMoveSpeed`。
- 升级奖励和被动选择可通过配置效果影响拾取吸附范围、投射物伤害倍率和移动速度。
- 被动选择优先读取 `TbRoguelikeRelic`，保留旧硬编码被动回退。

### 阶段 20：资源、音效与命中反馈最小接入

- 新增 `Roguelike_Hit.wav`、`Roguelike_Pickup.wav`、`Roguelike_LevelUp.wav` 三个最小音效资源。
- 命中、拾取和升级节点通过 `GameModule.Audio.Play(TEngine.AudioType.Sound, ...)` 安全触发音效。
- 音频播放带异常保护和短冷却，资源或模块异常时不影响局内流程。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有程序集引用版本冲突、AdditionalFile 和 nullable 注释警告。
- Unity Play Mode：`GameLogic.PlayModeTests` 19 项通过，0 失败。
- Unity Play Mode 冒烟：真实启动后 `Running`，战斗 UI 存在，配置读取到 3 个武器、3 个刷怪阶段、7 个被动，第三武器“穿透飞镖”和音频资源可用。

### 阶段 11：音效、特效与打击感

- 保持当前轻量原型方案，不新增独立特效框架或临时音频系统。
- `RoguelikeGame` 新增 `PickupFlash` 和 `CameraShake` 反馈状态，受击、击杀和拾取时触发，并随实时 Tick 衰减。
- `RoguelikeBattleStageView` 新增拾取脉冲视图和相机震动偏移，继续复用运行时生成 Sprite。
- 当前 `Assets/AssetRaw/Audios` 没有可用音频资源，因此不硬接空音效路径；正式音频后续应通过 TEngine `IAudioModule` 和资源模块接入。
- 新增 `RoguelikeStage11FeedbackTests`，覆盖命中反馈、拾取反馈、舞台反馈视图和反馈计时衰减。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有程序集引用版本冲突、AdditionalFile 和 nullable 注释警告。
- Unity Play Mode：`GameLogic.PlayModeTests` 14 项通过，0 失败，控制台 0 错误。
- 阶段 11 状态更新为“已完成”。阶段 15 已先完成规划，当前路线文档内阶段已全部推进完毕。

### 阶段 15：内容扩展计划

- 梳理当前内容基线：敌人表 6 条数据、升级选项表 19 条数据、被动表 4 条数据，运行时武器 2 种。
- 明确下一轮扩展优先级：先清理并补齐 Luban 表结构，再分阶段扩展敌人、刷怪节奏、第三武器、被动效果和资源反馈。
- 阶段 15 只输出计划和实施拆分，不直接新增大批内容，避免把多个后续阶段混入一次改动。
- 建议后续拆成阶段 16-20：配置表补齐、敌人刷怪扩展、第三武器、被动配置解释、资源音效反馈。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有程序集引用版本冲突和 AdditionalFile 警告。
- Unity Play Mode：当前阶段无运行时代码改动，沿用阶段 14 的 `GameLogic.PlayModeTests` 11 项通过和控制台 0 错误基线。
- Unity MCP：`editor/state` 显示 `ready_for_tools=true`，控制台 0 错误。
- `git diff --check`：通过，仅输出工作区换行符提示。
- 阶段 15 状态更新为“已完成”，后续补完阶段 11 后当前路线文档内阶段才视为全部推进完毕。

### 阶段 14：地图边界与拾取体验

- 复核现有地图边界、掉落物吸附和拾取逻辑，确认当前实现满足阶段 14 最小目标。
- 新增 `RoguelikeStage14BoundaryPickupTests`，覆盖玩家位置被限制在地图边界内，以及经验掉落物靠近玩家后吸附并收集入账。
- 阶段 14 未新增新地图系统、掉落配置表或复杂拾取表现，避免提前进入内容扩展阶段。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有程序集引用版本冲突、AdditionalFile 和 nullable 注释警告。
- Unity Play Mode：`GameLogic.PlayModeTests` 11 项通过，0 失败，控制台 0 错误。
- `git diff --check`：通过，仅输出工作区换行符提示。
- 阶段 14 状态更新为“已完成”，下一阶段进入阶段 15：内容扩展计划。

### 阶段 13：Luban 配置化

- 复用现有 Luban 生成配置和二进制数据，不新增第二套配置系统。
- `RoguelikeGame` 新增配置表读取缓存：运行时可从 `ConfigSystem.Instance.Tables` 获取 `TbRoguelikeEnemy` 和 `TbRoguelikeChoice`。
- 生存刷怪优先使用敌人配置表的基础生命和攻击，并继续叠加当前波次成长。
- 升级三选一会从配置表读取基础奖励效果，当前表内中文存在旧编码痕迹，因此用户可见标题和描述先保留代码侧中文映射。
- 配置不可用时保留原有硬编码回退，避免测试环境或资源模块未初始化时阻塞局内流程。
- PlayMode 测试程序集补充 `GameProto` 引用，新增 `RoguelikeStage13ConfigTests` 覆盖配置二进制加载、配置效果应用和配置敌人刷怪。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有程序集引用版本冲突和 AdditionalFile 警告。
- Unity Play Mode：`GameLogic.PlayModeTests` 9 项通过，0 失败，控制台 0 错误。
- `git diff --check`：通过，仅输出工作区换行符提示。
- 阶段 13 状态更新为“已完成”，下一阶段进入阶段 14：地图边界与拾取体验。

### 阶段 9：对象池与性能优化

- 检查 TEngine `ObjectPoolModule` 和 `MemoryPool` 后，阶段 9 选择轻量接入 `MemoryPool`，不新增第二套全局对象池管理器。
- 敌人、掉落物和投射物实现 `IMemory`，运行时生成改为从 TEngine `MemoryPool` 获取。
- 重开、敌人死亡、投射物命中或耗尽、掉落物拾取时归还池化对象，并保留旧构造兼容既有测试辅助代码。
- `RoguelikeBattleStageView` 新增敌人、掉落物和投射物 Transform 复用栈，临时视图消失后进入本舞台复用池。
- 视图组件获取改为显式 Unity 判空，避免 `UnityEngine.Object` 假 null 与 C# `??` 混用导致池化对象复用不稳定。
- 新增 `RoguelikeStage9PoolingTests`，覆盖运行时对象归还 TEngine `MemoryPool` 和舞台临时视图复用。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有程序集引用版本冲突、AdditionalFile 和 nullable 注释警告。
- Unity Play Mode：`GameLogic.PlayModeTests` 6 项通过，0 失败，控制台 0 错误。
- `git diff --check`：通过，仅输出工作区换行符提示。
- 阶段 9 状态更新为“已完成”，下一阶段进入阶段 13：Luban 配置化。

### 阶段 8：被动道具与构筑

- 升级三选一新增少量被动道具：“磁力核心”“聚能护符”“疾风靴”。
- 被动道具复用现有 `RoguelikeRelicTemplate` 和 `CurrentRun.Relics`，不新增复杂词条、稀有度或技能树系统。
- “磁力核心”提升拾取吸附范围，“聚能护符”提升投射物伤害，“疾风靴”提升移动速度。
- HUD 新增被动汇总显示，重复获得同一被动时显示叠加数量。
- 新增 `RoguelikeStage8PassiveTests`，覆盖被动选项、运行时属性生效、被动汇总和投射物伤害加成。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留既有程序集引用版本冲突、AdditionalFile 和 nullable 注释警告。
- Unity Play Mode：`GameLogic.PlayModeTests` 4 项通过，0 失败，控制台 0 错误。
- `git diff --check`：通过，仅输出工作区换行符提示。
- 阶段 8 状态更新为“已完成”，下一阶段进入阶段 9：对象池与性能优化。

### 阶段 7：多武器与武器升级

- 新增运行时武器状态列表，开局默认持有“追踪魔弹”。
- 原自动索敌投射物改为由“追踪魔弹”武器驱动，并随武器等级提升伤害、射程和冷却。
- 新增第二武器“旋刃”，解锁后会按独立冷却向周围环形齐射短程刀刃。
- 升级三选一加入武器相关选项：魔弹升级、解锁旋刃、旋刃升级。
- HUD 显示当前武器与等级，魔弹和旋刃投射物使用不同视觉颜色。
- 新增 `GameLogic.PlayModeTests`，覆盖武器选项解锁、武器升级和多武器同时发射。
- Unity MCP 已恢复可用，`editor/state` 显示 `ready_for_tools=true`，并可执行 `read_console`、`find_gameobjects`、`run_tests`。
- `dotnet build UnityProject\UnityProject.sln --no-restore`：通过，0 错误；保留 2 个既有程序集引用版本冲突警告。
- Unity Play Mode：`GameLogic.Tests.RoguelikeStage7WeaponTests` 2 项通过，0 失败，控制台 0 错误。
- `git diff --check`：通过，仅输出工作区换行符提示。
- 阶段 7 状态更新为“已完成”，下一阶段进入阶段 8：被动道具与构筑。

## 2026-06-04

### 当前产品方向

- 项目方向明确为类《吸血鬼幸存者》的俯视角 2D 自动战斗肉鸽。
- 开发改为严格按阶段推进，当前阶段未完成 Unity 运行验收前不进入下一阶段。
- 新增 [类吸血鬼幸存者阶段开发路线.md](D:/Work/Unity_Project/TEngine_Game/Doc/类吸血鬼幸存者阶段开发路线.md:1)。

### 当前原型状态

- 已实现 WASD 移动、边缘刷怪、敌人追踪、接触伤害、范围攻击、掉落拾取、经验升级、随机三选一、难度成长、HUD、结算和永久金币。
- 已知偏差：移动未使用 `Rigidbody2D`；攻击尚未实现自动搜敌武器系统。
- 当前优先推进阶段 1：玩家移动正式化与 Unity Play Mode 验收。

### 项目规范

- 新增根目录 `AGENTS.md`。
- 功能实现前优先检查并复用 TEngine 和项目已有方案。
- 代码保持简洁、高效，不为未确认需求过度设计。
- 重要文档使用中文名称和中文内容。

### 文档清理

- 删除与当前自动战斗生存路线冲突的旧轻操作房间制排期和节点文档。
- 精简文档索引、系统总览、MVP 说明和开发日志，仅保留当前有效结论。

### 验证

- `dotnet build UnityProject.sln --no-restore`：通过，0 错误。
- `git diff --check`：通过。
- Unity 编辑器最近日志未发现新增脚本编译或空引用错误。

### 阶段 1：玩家移动正式化

- 玩家移动已从逻辑坐标直接累加改为 `Rigidbody2D.MovePosition` 驱动。
- 玩家运行时对象已挂载 `Rigidbody2D` 与 `CircleCollider2D`，并保留速度、暂停和地图边界规则。
- 新增主摄像机平滑跟随，修正运行时舞台重建时主摄像机可能被清理的问题。
- 重开一局时，玩家物理位置会同步回本局初始位置。
- Unity Play Mode 验收通过：移动状态与物理位置一致，暂停不移动，边界限制为 `x=7.50`，重开后回到原点，摄像机正常跟随。
- Unity 控制台 0 错误，阶段 1 完成，进入阶段 2。

### 阶段 2-3：敌人追踪与受伤验收

- 敌人运行时视图加入运动学 `Rigidbody2D`、触发碰撞体和物理帧位置同步。
- Play Mode 中敌人数量从 2 增至 3，首个敌人与玩家距离从 `4.99` 降至 `3.40`。
- 接触伤害验收将玩家生命从 `100` 降至 `92`，生命与受伤链路正常。
- Unity 控制台 0 错误，阶段 2 与阶段 3 完成，进入阶段 4。

### 阶段 4：自动攻击武器

- 将方向范围攻击替换为最近敌人自动索敌和直线投射物。
- 投射物包含速度、伤害、剩余射程，并有独立可见视图。
- Play Mode 中目标生命从 `23` 降至 `11`，随后被投射物击杀，击杀数变为 `1`。
- Unity 控制台 0 错误，阶段 4 完成，进入阶段 5 与阶段 6 验收。

### 阶段 5-6：升级与难度曲线验收

- 升级后进入 `RewardChoice`，局内时间冻结，并生成 3 个不同强化选项。
- 选择强化后恢复运行，本次验证移动速度从 `4.50` 提升到 `4.95`。
- `0.4` 秒时敌人生命、攻击、速度为 `23/9/1.43`；`120.4` 秒后为 `43/17/1.75`，单次刷怪数从 1 增至 3。
- Unity 控制台 0 错误，阶段 5 与阶段 6 完成。

### 阶段 10/12：UI 与完整流程验收

- HUD、升级三选一和结算界面完成截图复验。
- 修复小尺寸按钮标签高度不足导致文字被裁切的问题，结算重开按钮已正常显示。
- 操作提示更新为“武器自动攻击最近敌人”，移除过期的方向攻击说明。
- 死亡后进入 `Defeated`；重开后恢复 `Running`、生命 `100`、等级 `1`。
- Unity 控制台 0 错误，阶段 10 与阶段 12 完成。
