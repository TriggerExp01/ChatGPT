# 《星蚀幸存者》阶段开发路线

## 项目目标

使用 Unity 和当前 TEngine 项目实现一款暂定名为《星蚀幸存者》的 2D 俯视角自动战斗肉鸽游戏。

游戏发生在被“星蚀”污染的古老遗迹中。玩家扮演轻幻想风格的冒险者 / 星辉猎人，通过走位、拾取、升级奖励选择和构筑规划，在不断增强的敌潮中存活并击败本局 Boss。整体风格走明亮、奇幻、带一点神秘感的二次元轻幻想 Bullet Heaven，不走暗黑写实，也不走廉价手游堆按钮风格。

核心体验：

- 玩家使用 WASD 或虚拟方向控制角色移动、闪避和拾取。
- 武器自动索敌、发射、命中并造成伤害，玩家不需要频繁瞄准和攻击。
- 敌人从地图四周持续涌入，随时间推进提高数量、生命、攻击、速度和刷怪组合强度。
- 击杀敌人掉落经验和金币，经验升级进入三选一奖励界面。
- 玩家选择武器升级、解锁新武器、获得被动或购买局内补给，形成每局构筑。
- 关键时间点出现精英敌人和 Boss，击败 Boss 后进入胜利结算，死亡则进入失败结算。
- 结算获得永久金币或成长资源，用于下一局的轻量局外成长。

目标体验：

- 战斗要有“越打越密、越升越爽”的弹幕生存感。
- 每局前 1 分钟快速进入状态，3 分钟内看到明显构筑成型，5 到 10 分钟形成敌潮压迫和 Boss 目标。
- 至少形成 4 种定位清晰的武器：追踪魔弹、旋刃、穿透飞镖、星环脉冲。
- 至少形成 6 种以上被动方向：攻击、移动、拾取、生命 / 恢复、暴击、金币收益，后续再扩展冷却缩减、投射物数量和经验倍率。
- UI 清晰显示生命、经验、等级、金币、时间、击杀数、当前武器、当前被动和关键提示。
- 升级三选一要有卡牌感，让玩家一眼看出奖励类型、效果、价格和是否可选。
- 命中、暴击、击杀、拾取、升级、Boss 出场、胜利和失败都要有可感知反馈。
- 第一版地图使用固定矩形地图，有边界和少量障碍，不提前做随机地图、复杂寻路或多关卡系统。
- 第一版局外成长只做永久攻击力、初始生命或金币收益等轻量成长，不做复杂装备、背包、抽卡、角色养成或商业化入口。

最终成品标准：

- 玩家能完整进行一局游戏，从开局、战斗、升级、构筑、Boss、胜利或死亡到结算和重开。
- 至少有 4 种武器、6 种以上被动、5 种以上敌人、1 个 Boss、1 条完整刷怪节奏。
- 所有核心行为都有 UI 显示和基础反馈。
- Play Mode 验收中核心流程稳定，控制台无新增 Error。
- 画面能用于演示和截图，至少具备 Steam 商店页早期展示潜力。
- 项目继续遵守每次只推进一个阶段、通过 Unity MCP 验收后再进入下一阶段的开发方式。

## 执行规则

1. 每次只推进一个阶段。
2. 当前阶段完成 Unity 运行验收后，再进入下一阶段。
3. 优先复用 TEngine 与当前项目已有模块。
4. 代码保持简洁，不为后续阶段提前建设复杂系统。
5. 对象池、Luban 配置化和内容扩展在核心闭环稳定后再推进。

## 当前阶段状态

| 阶段 | 状态 | 当前结论 |
| --- | --- | --- |
| 阶段 0：基础规则与约束 | 已完成 | 已建立根目录项目规范、框架优先和简洁代码规则 |
| 阶段 1：玩家移动 | 已完成 | `Rigidbody2D.MovePosition`、地图边界、暂停、重开归零和摄像机跟随均已通过 Play Mode 验收 |
| 阶段 2：敌人生成与追踪 | 已完成 | 已实现四周刷怪、逻辑追踪、`Rigidbody2D` 视图同步和触发碰撞体，并通过 Play Mode 验收 |
| 阶段 3：生命值与受伤 | 已完成 | 生命值、接触伤害间隔、死亡与结算链路已通过 Play Mode 验收 |
| 阶段 4：自动攻击武器 | 已完成 | 已实现最近敌人索敌、可见投射物、命中伤害和射程销毁，并通过 Play Mode 验收 |
| 阶段 5：经验、等级与三选一 | 已完成 | 经验升级、时间冻结、随机三选一和强化应用已通过 Play Mode 验收 |
| 阶段 6：时间与难度曲线 | 已完成 | 时间推进、刷怪数量和敌人生命、攻击、速度成长已通过 Play Mode 验收 |
| 阶段 7：多武器与武器升级 | 已完成 | 第二武器、武器解锁、武器升级和多武器同时执行已通过 Play Mode 测试验收 |
| 阶段 8：被动道具与构筑 | 已完成 | 已新增少量被动道具、被动汇总和 Play Mode 验收 |
| 阶段 9：对象池与性能优化 | 已完成 | 敌人、掉落物、投射物运行时对象已接入 TEngine `MemoryPool`，舞台临时视图已复用并通过 Play Mode 验收 |
| 阶段 10：基础 UI 与反馈 | 已完成 | HUD、升级三选一和结算 UI 已通过截图与 Play Mode 验收，按钮文字裁切已修复 |
| 阶段 11：音效、特效与打击感 | 已完成 | 已新增受击闪白、拾取脉冲和相机震动，正式音效待音频资源补齐后接入 TEngine AudioModule |
| 阶段 12：完整局内流程与结算 | 已完成 | 开始、游玩、升级、死亡、结算和重开流程已通过 Play Mode 验收 |
| 阶段 13：Luban 配置化 | 已完成 | 已复用现有 Luban 敌人表和升级选项表，完成最小配置化接入与 Play Mode 验收 |
| 阶段 14：地图边界与拾取体验 | 已完成 | 地图边界、掉落物吸附和拾取收集已通过 Play Mode 验收 |
| 阶段 15：内容扩展计划 | 已完成 | 已形成下一轮内容扩展优先级、配置边界和验收拆分 |
| 阶段 16：配置表清理与表结构补齐 | 已完成 | 已修复 Luban 表中文文本，补齐武器表、被动效果和刷怪阶段表，并完成生成链路验证 |
| 阶段 17：敌人与刷怪节奏配置化扩展 | 已完成 | 已新增刷怪阶段配置读取，普通敌人权重、精英/Boss 节点和生命成长从配置驱动 |
| 阶段 18：第三武器最小实现 | 已完成 | 已新增“穿透飞镖”，复用现有投射物、对象池和舞台视图，并接入升级池 |
| 阶段 19：被动效果配置解释层 | 已完成 | 已将磁力、投射物伤害和移动速度等被动效果接入配置解释层 |
| 阶段 20：资源、音效与命中反馈最小接入 | 已完成 | 已新增最小 WAV 音效资源，并在命中、拾取和升级节点通过 TEngine 音频模块安全触发 |
| 阶段 21：敌人与战斗内容第二轮扩展 | 已完成 | 已新增星尘游灵、蘑菇守卫、晶壳虫和月影骑士，并接入刷怪阶段配置与 Play Mode 测试 |
| 阶段 22：刷怪节奏与时间目标细化 | 已完成 | 已将 0-10 分钟刷怪节奏细化为 5 段，Boss 节点固定在 600 秒，并通过 Play Mode 测试覆盖阶段切换和流程稳定性 |
| 阶段 23：武器配置与数值平衡第一轮 | 已完成 | 已通过 `roguelike_weapon.xlsx` 微调追踪魔弹、旋刃和穿透飞镖，明确单体稳定、近身群体和远程穿透定位 |
| 阶段 24：第四武器最小实现 | 已完成 | 已新增低复杂度武器“星环脉冲”，复用现有投射物、舞台视图和对象池，不引入武器进化 |
| 阶段 25：被动道具第二轮扩展 | 已完成 | 已新增生息符文、鹰眼纹章和星辉轻靴，全部复用现有被动效果解释层与 HUD 汇总显示 |
| 阶段 26：掉落与金币用途最小闭环 | 已完成 | 已将局内金币接入升级三选一付费补给、余额不足阻止选择和结算入账闭环 |
| 阶段 27：局内目标与胜利条件扩展 | 已完成 | 已固化“击败地牢之心”作为最小胜利目标，并打通胜利结算与重开流程 |
| 阶段 28：Boss 战第一轮可玩化 | 已完成 | 已强化 Boss 出场提示、震屏反馈、体型/颜色表现和击败金币奖励，不新增复杂 Boss 技能系统 |
| 阶段 29：地图空间体验第一轮 | 已完成 | 已在固定地图中加入轻量障碍、简单阻挡和障碍表现，保持固定场地与简单碰撞 |
| 阶段 30：HUD 信息密度与二次元轻幻想表现打磨 | 已完成 | 已将局内 HUD 调整为低遮挡分区布局，补齐角色状态、时间压力、构筑槽和底部提示表现 |
| 阶段 31：升级三选一卡牌体验打磨 | 已完成 | 已将三选一升级打磨为二次元轻幻想卡牌界面，补齐图标、标题、描述、价格、不可选状态和选择反馈 |
| 阶段 32：结算与局外成长最小实现 | 已完成 | 已将永久金币成长展示为结算面板中的数据、收益和进度闭环，并接入下局初始攻击加成 |
| 阶段 33：正式音效替换与混音规则 | 已完成 | 已补齐 Boss/UI 音效资源，整理五类音效地址、音量和独立短冷却规则，继续走 TEngine AudioModule |
| 阶段 34：命中特效与拾取特效 Prefab 接入 | 已完成 | 已接入命中、击杀和拾取三类轻量 Prefab 特效，复用 TEngine 资源模块并保留运行时 Sprite 回退 |
| 阶段 35：敌人和投射物 Prefab 化第一轮 | 已完成 | 已接入普通敌人、Boss、魔弹和穿透飞镖 Prefab 表现，保留运行时 Sprite 回退与视图池复用 |
| 阶段 36：配置校验与生成流程加固 | 已完成 | 已新增肉鸽配置完整性校验、资源地址检查和生成后验证说明，并通过 Play Mode 验收 |
| 阶段 37：性能基线与 GC 观测 | 已完成 | 已建立固定步进性能快照、对象池计数、托管内存和视图池观测口径 |
| 阶段 38：对象池与大量敌人压测 | 已完成 | 已新增高密度运行时对象压测入口，验证对象池释放、视图入池和复用稳定 |
| 阶段 39：新手引导与操作提示最小实现 | 已完成 | 已复用现有 HUD、操作条、升级卡牌和结算面板，接入开局、升级、结算和重开提示 |
| 阶段 40：版本验收与演示包准备 | 已完成 | 已汇总阶段 21-39 的验收结果，整理演示说明、截图清单、可演示版本和下一轮边界 |
| 阶段 41：伤害数字与暴击反馈表现 | 已完成 | 已在命中事件中携带伤害与暴击信息，舞台视图生成可回收飘字伤害数字并提升命中/击杀特效可读性 |
| 阶段 42：视觉资产来源与许可清单 | 已完成 | 已把 HUD 滑条和光带替换为肉鸽专用原创 UI 资源，并建立关键视觉资产来源、许可和用途清单 |
| 阶段 43：奖励卡图标免费资源替换第一轮 | 已完成 | 已精选导入 Kenney Game Icons 的 CC0 图标，并让奖励卡按武器、被动和付费补给映射更正式的图标 |
| 阶段 44：暴击特效分层与反馈强化 | 已完成 | 已将暴击命中拆出独立表现 cue，叠加更醒目的暴击特效、飘字和震屏反馈，且不重复生成伤害数字 |
| 阶段 45：Kenney 粒子贴图特效替换第一轮 | 已完成 | 已精选导入 Kenney Particle Pack 的 CC0 透明粒子贴图，替换命中、暴击、击杀和拾取特效 Prefab 的基础 Sprite |
| 阶段 46：展示战斗构图模式第一轮 | 已完成 | 已新增固定种子的自然战斗展示构图入口，生成非矩阵敌群、弹幕、掉落、伤害数字和暴击/拾取/击杀特效，用于后续 Steam 截图制作 |
| 阶段 47：展示截图 UI 控制第一轮 | 已完成 | 已新增展示截图模式，隐藏底部操作提示和 HUD 提示条，降低上方 HUD 遮挡，并保持正常游玩 UI 不受影响 |
| 阶段 48：HUD 构筑槽图标正式化第一轮 | 已完成 | 已复用 Kenney Game Icons 的 CC0 图标，让 HUD 右上构筑槽按具体武器和被动类型显示正式图标 |
| 阶段 49：伤害数字细节表现强化第二轮 | 已完成 | 已为伤害飘字补充阴影、重击档位和暴击更长上浮表现，提升高密度战斗中的伤害可读性 |
| 阶段 50：掉落物/拾取物正式表现第一轮 | 已完成 | 已复用已记录的金币徽章与 Kenney CC0 成长图标，让经验和金币掉落物从运行时色块升级为正式 Sprite 表现 |
| 阶段 51：升级奖励卡面正式底纹第一轮 | 已完成 | 已复用现有项目自制 UI 资产，为升级奖励卡补充正式底纹、类别徽带、图标底座、金币徽章和不可选遮罩 |
| 阶段 52：外部免费 UI 框体与提示装饰接入第一轮 | 已完成 | 已精选导入 Kenney Fantasy UI Borders 的 CC0 面板、卡牌、按钮和分隔装饰，并接入 HUD、操作提示、升级卡和结算 UI |
| 阶段 53：玩家与角色正式视觉表现第一轮 | 已完成 | 已精选导入 Kenney Roguelike Characters 的 CC0 玩家角色与头像 Sprite，并接入 HUD、结算和局内玩家表现 |
| 阶段 54：敌人正式视觉表现第一轮 | 计划中 | 计划按普通敌人、精英敌人和 Boss 类型替换更明确的免费或自制表现资源，保留当前刷怪与对象池逻辑 |
| 阶段 55：命中、升级与拾取特效表现强化第三轮 | 计划中 | 计划继续使用免费粒子贴图或项目自制特效，强化命中、升级、拾取和击杀反馈，不引入付费插件依赖 |
| 阶段 56：HUD 与提示动效反馈第一轮 | 计划中 | 计划为 HUD、提示、奖励卡和按钮补轻量动效/节奏反馈，优先项目内实现，必要时再评估 Feel 等付费插件 |
| 阶段 57：外部 UI 正式资产替换第二轮 | 已完成 | 已按用户选定的 Tiny RPG + RPG UI 方向，先接入 System G6 的 CC0 RPG UI 大面板、按钮和进度条，Tiny RPG 记录为待官方 zip 获取后补充 |
| 阶段 58：UI 槽位正式框体绑定 | 已完成 | 已将 System G6 的正式槽位框接入 HUD 构筑槽和升级奖励卡图标底座，让已导入槽框成为真实可见 UI 资产 |
| 阶段 59：UI 金币徽章正式框体绑定 | 已完成 | 已将 System G6 的正式槽位框继续接入升级卡金币花费徽章和结算永久成长金币徽章，减少裸图标占位感 |
| 阶段 60：UI 提示面板正式装饰线绑定 | 已完成 | 已将 System G6 金属分隔装饰线接入 HUD 战斗提示面板，让底部提示条与操作提示、HUD 面板保持统一正式框体语言 |
| 阶段 61：UI 奖励卡不可选遮罩正式框体绑定 | 已完成 | 已将升级奖励卡不可选遮罩从项目自制面板框切换为 System G6 正式卡框，让金币不足状态也保持统一 RPG UI 卡牌语言 |
| 阶段 62：UI 奖励卡顶部徽带正式装饰绑定 | 已完成 | 已将升级奖励卡顶部徽带从自制蓝色进度条切换为 System G6 金属分隔装饰，保留奖励类别着色但减少占位感 |
| 阶段 63：UI 奖励卡内层描边正式框体绑定 | 已完成 | 已将升级奖励卡内层描边从自制光带切换为 System G6 正式卡框，让奖励卡内部装饰和外框/遮罩保持统一 |
| 阶段 64：RoguelikeGame 职责梳理与最小结构拆分 | 已完成 | 已将刷怪阶段解析、敌人配置选择、Boss 一次性刷出状态和敌人移动速度解释拆入 `RoguelikeSpawnDirector`，保留 `RoguelikeGame` 原私有包装方法兼容既有入口 |
| 阶段 65：奖励池构建职责拆分 | 已完成 | 已将升级三选一候选池构建、配置奖励读取、武器奖励项、被动奖励项和三选一抽取拆入 `RoguelikeRewardDirector`，保留 `RoguelikeGame` 原私有包装方法兼容既有入口 |
| 阶段 66：武器运行时解释职责拆分 | 已完成 | 已将武器冷却、目标选择、投射物发射参数和四种武器发射解释拆入 `RoguelikeWeaponDirector`，保留 `RoguelikeGame` 原私有包装方法兼容既有入口 |
| 阶段 67：主策划接收《星蚀幸存者》成品描述并重编路线 | 已完成 | 已将用户给出的产品描述固化为路线文档中的产品目标、当前阶段判断、阶段合同和后续推荐顺序，本阶段不修改玩法代码 |
| 阶段 68：敌人定位与外观第一轮补齐 | 已完成 | 已为普通、快速、高生命、精英和 Boss 五类敌人补齐运行时角色分类、颜色和体型差异，优先提升局内可读性和截图价值 |
| 阶段 69：被动定位与构筑差异第一轮 | 计划中 | 计划围绕攻击、移动、拾取、生命恢复、暴击和金币收益补齐 6 种以上被动定位，不引入复杂词条系统 |
| 阶段 70：5-10 分钟局内节奏与 Boss 目标复核 | 计划中 | 计划按 0-1、1-3、3-6、6-10 分钟复核刷怪密度、升级速度、构筑成型和 Boss 触发目标 |
| 阶段 71：拾取与金币体验职责拆分 | 计划中 | 计划继续结构收口，把拾取吸附、收集入账和金币补给反馈从 `RoguelikeGame` 中拆出单一辅助边界 |
| 阶段 72：Boss 出场与胜利反馈第二轮 | 计划中 | 计划强化 Boss 出场提示、特殊音效/震屏、胜利目标感和胜利结算表现，不提前做复杂 Boss 技能 |
| 阶段 73：演示截图固定分辨率输出 | 计划中 | 计划建立用于 Steam 早期展示图的固定分辨率截图输出和取景清单，避免临时手工截图不可复现 |
| 阶段 74：局外成长第二轮最小扩展 | 计划中 | 计划在现有永久金币攻击加成基础上评估初始生命或金币收益成长，保持轻量且不压过局内构筑 |
| 阶段 75：星蚀主题演示版验收包 | 计划中 | 计划汇总《星蚀幸存者》核心闭环、截图、验证结果、可演示版本说明和下一轮内容边界 |

## 当前推进边界

【总策划 / 制作人 Agent 接收结论】

用户给出的《星蚀幸存者》成品描述已经被接收为后续阶段路线的主产品合同。当前项目不是从零开始：阶段 0-66 已经完成大量核心闭环、内容扩展、UI 表现、资源许可、展示构图和结构拆分工作。后续不需要重做基础移动、自动攻击、经验升级、三选一、Boss 胜利、结算、对象池、基础 UI 或四种武器，而应围绕“成品描述中尚未完全达标的体验缺口”继续逐阶段收口。

当前已达成的成品标准：

- 核心闭环：开局、移动、自动攻击、刷怪、击杀、经验、升级、构筑、Boss、胜利 / 死亡结算和重开均已具备。
- 武器基线：追踪魔弹、旋刃、穿透飞镖、星环脉冲 4 种武器已接入，阶段 66 已将武器运行时解释拆入 `RoguelikeWeaponDirector`。
- 敌人和节奏基线：已有 5 种以上敌人、精英 / Boss 节点、0-10 分钟刷怪阶段和 Boss 胜利目标。
- 被动与金币基线：已有攻击、移动、拾取、投射物伤害、生命 / 恢复、金币补给和永久金币成长等基础闭环。
- UI 基线：HUD、升级三选一卡牌、结算、构筑槽、提示条、金币徽章和正式 RPG UI 框体已具备可读基础。
- 反馈基线：命中、暴击、击杀、拾取、升级、Boss、UI 确认和胜利 / 失败均已有最小音效或视觉反馈。
- 工程基线：对象池、配置校验、Play Mode 回归、Unity MCP 验收和阶段 commit 流程已经固化。

当前最主要缺口：

1. 成品主题“星蚀污染遗迹 / 星辉猎人”已经在描述中明确，但敌人外观和部分表现仍偏通用占位，缺少普通、快速、高生命、精英、Boss 的强可读差异。
2. 被动数量和定位需要按成品描述重新梳理，确保至少 6 种以上被动不只是数值加减，而是能服务构筑判断。
3. 5-10 分钟局内节奏需要再次按“前 1 分钟入状态、3 分钟构筑成型、5-10 分钟高压与 Boss”复核，而不是只满足测试阈值。
4. `RoguelikeGame` 的拾取、反馈、展示构图和结算存档职责仍可继续小步拆分。
5. Steam 早期展示潜力仍需要固定分辨率截图输出、敌人正式视觉和更稳定的展示取景。

下一阶段允许推进：阶段 68“敌人定位与外观第一轮补齐”。原因是《星蚀幸存者》的核心玩法闭环和四武器基线已经存在，后续最影响玩家第一眼理解和截图价值的是敌人类型可读性；同时该阶段可以只处理敌人定位、配置与表现，不跨入被动、节奏、Boss 技能或截图系统。

阶段 67 本身只做主策划接收和路线文档重编，不修改代码、资源、配置表或 Unity 场景；因此阶段 67 的完成标准是本路线文档更新、开发日志记录和文档级验证通过。

阶段 68 已完成敌人定位与外观第一轮补齐：不修改 Luban 表、不改刷怪节奏、不新增复杂 AI 或 Boss 技能，只在运行时敌人模型中保存普通、快速、高生命、精英和 Boss 五类角色，并让舞台表现层按角色使用不同颜色和体型。当前推进边界顺延为阶段 69“被动定位与构筑差异第一轮”。

阶段 0-52 已完成，当前路线已形成一次可演示版本。正式 UI 资产第一轮、Steam 展示图方向验证、伤害数字与暴击反馈表现、视觉资产来源与许可清单、奖励卡图标免费资源替换第一轮、暴击特效分层与反馈强化、Kenney 粒子贴图特效替换第一轮、展示战斗构图模式第一轮、展示截图 UI 控制第一轮、HUD 构筑槽图标正式化第一轮、伤害数字细节表现强化第二轮、掉落物/拾取物正式表现第一轮、升级奖励卡面正式底纹第一轮、外部免费 UI 框体与提示装饰接入第一轮已完成。当前项目已经具备可复现的自然战斗展示状态，并可在截图模式下降低非必要 UI 遮挡；右上构筑槽也已能用具体武器和被动类型图标强化可读性，伤害飘字已区分普通、重击和暴击表现，经验/金币掉落也已使用正式 Sprite 替代纯色菱形占位，升级三选一卡面与 HUD/提示/结算面板也已接入 Kenney CC0 外部框体、按钮和分隔装饰。最终 Steam 商店图仍需正式角色立绘、敌人外观、固定分辨率截图输出和进一步正式特效资产补齐。

阶段 53 已在本轮完成：HUD、结算和局内玩家已接入 Kenney Roguelike Characters 的 CC0 角色 Sprite，当前推进边界顺延为阶段 0-53 已完成；后续重点转向阶段 54 敌人正式视觉表现、阶段 55 特效强化和阶段 56 HUD/提示动效。

阶段 57 已按最新 UI 美术方向插队完成：当前 Kenney 第一轮外部框体已被 System G6 的 CC0 RPG UI 大面板、按钮、金属分隔和进度条替换，HUD、操作提示、升级卡和结算的基础框体正式感提升；Tiny RPG - Mana Soul GUI 已作为用户选定的像素 HUD 小控件补充包记录到许可清单，但本轮未使用预览图或非最终 zip 资源，待官方 zip 获取后再补充导入。

阶段 58 已继续补齐 UI 资产正式落地：阶段 57 已导入但未绑定的 `Roguelike_SystemG6_UI_SlotFrame` 现在用于 HUD 右上武器/被动构筑槽外框和升级奖励卡图标底座，现有正式图标仍保留在槽框内部；本阶段同时复查 Tiny RPG itch 官方下载页，确认上传 ID `13498579` 和 94 kB zip 文件列表，但 `/file/13498579` 端点当前仍返回 `invalid key`，因此继续禁止使用预览图替代工程资产。

阶段 59 已在不新增资源、不改交互流程的前提下继续推进 UI 正式资产落地：`Roguelike_SystemG6_UI_SlotFrame` 现在也用于升级奖励卡付费金币徽章外框和结算永久成长金币徽章外框，内部仍使用既有 `Roguelike_UI_GoldIcon`，让金币相关小徽章与 HUD 构筑槽、奖励图标底座保持同一 RPG UI 框体语言。

阶段 60 已继续在 HUD 可见层消化已导入的 RPG UI 资源：`BattleHudUI` 的 `战斗提示面板` 新增 `战斗提示外部装饰线`，绑定 `Roguelike_SystemG6_UI_DividerMetal`，不新增资源、不修改提示文案、不改变截图模式隐藏逻辑，只提升底部提示面板的正式装饰层级。

阶段 61 已继续收口升级奖励卡中的自制占位残留：`BattleChoiceUI` 的 `卡牌不可选遮罩_{n}` 从 `Roguelike_UI_PanelFrame` 切换为 `Roguelike_SystemG6_UI_CardFrame`，保留原有遮罩透明度、金币不足判定和层级逻辑，只让不可选状态也使用当前正式 RPG UI 卡框。

阶段 62 已继续处理升级奖励卡的自制装饰残留：`BattleChoiceUI` 的 `卡牌顶部徽带_{n}` 从 `Roguelike_UI_SliderFill_Blue` 切换为 `Roguelike_SystemG6_UI_DividerMetal`，保留武器/被动/金币不足的类别着色和淡化逻辑，只把顶部条从“进度条占位”推进为正式金属装饰。

阶段 63 已继续收口升级奖励卡内部仍偏自制的描边残留：`BattleChoiceUI` 的 `卡牌边框_{n}` 从 `Roguelike_UI_LightBand` 切换为 `Roguelike_SystemG6_UI_CardFrame`，让奖励卡根框、内层描边和不可选遮罩使用同一套正式卡框语言。

本次已固化 UI 质量提升办法：后续正式定 UI 风格前，必须先提供或收集目标参考图，再拆解色板、字体层级、按钮形态、面板密度、HUD 遮挡率和卡牌样式；UI 实现优先沉淀可复用组件，并通过 Unity MCP 截图级验收确认观感。用户已明确后续暂不继续顶 UI 风格，因此下一阶段建议从 UI 抛光切回功能承载能力与结构债务收口。

阶段 64 已完成第一轮最小结构拆分：新增 `RoguelikeSpawnDirector`，将刷怪阶段选择、阶段内 Boss/精英/普通敌人配置选择、加权敌人池选择和敌人移动速度解释从 `RoguelikeGame` 中拆出。`RoguelikeGame` 保留 `GetActiveSpawnStage`、`PickEnemyConfig`、`PickWeightedEnemy` 和 `GetEnemyMoveSpeed` 私有包装方法，继续兼容阶段 16-22 既有反射回归与当前 UI/舞台调用。该阶段不新增敌人、武器、奖励、UI 风格、外部资源或复杂架构。

阶段 65 已完成第二轮最小结构拆分：新增 `RoguelikeRewardDirector`，将升级三选一候选池构建、配置奖励读取、武器奖励项生成、被动奖励项生成和三选一去重抽取从 `RoguelikeGame` 中拆出。`RoguelikeGame` 保留 `BuildLevelUpOptions`、`AddConfiguredRewardChoices`、`AddWeaponChoice`、`AddPassiveChoices`、`AddConfiguredPassiveChoices` 和 `AddPassiveFromConfig` 私有包装方法，继续兼容既有阶段 25、31 等反射回归。本阶段不改变奖励内容、奖励数值、金币补给、UI 风格、武器升级规则或配置表。

阶段 66 已完成第三轮最小结构拆分：新增 `RoguelikeWeaponDirector`，将武器冷却推进、目标选择、配置/回退数值解释、投射物发射参数和四种武器发射解释从 `RoguelikeGame` 中拆出。`RoguelikeGame` 保留 `UpdateWeapons`、`FireMagicBolt`、`FireSpinningBlade`、`FirePiercingDart`、`FireStarRingPulse`、`GetWeaponInterval`、`GetWeaponDamage`、`GetWeaponRange` 和 `GetWeaponSpeed` 私有包装方法，继续兼容既有反射回归和舞台/UI 访问。本阶段不改变武器内容、武器数值、配置表、投射物对象池、UI 风格或美术资源。

阶段 66 后的不足：`RoguelikeGame` 仍承担拾取吸附/收集、反馈 cue、展示构图和结算存档等职责。后续结构向阶段建议继续选择单一纯逻辑边界推进，例如“拾取反馈辅助”或“结算存档辅助”，不要一次性大拆单局主类。

后续推进必须继续遵守：

1. 第一批内容扩展优先补内容表和运行时最小解释逻辑，不改启动、热更或 UI 基础框架。
2. 每次只落地一个内容方向，例如敌人、武器、被动、刷怪节奏或资源表现中的一项。
3. 新增内容优先走 Luban 配置表；代码侧只保留运行时解释、发射模式和表现同步。
4. 每个实施小阶段仍需通过 `dotnet build`、`git diff --check` 和 Unity Play Mode 验收后再提交。
5. 展示图后续应优先补齐正式视觉资产和截图控制，不把高密度矩阵压测图直接当作最终商店页素材。
6. 后续表现阶段按“UI 框体 -> 玩家/角色 -> 敌人 -> 特效 -> HUD/提示动效”顺序推进；付费插件只作为后续评估项，当前默认继续使用免费或项目自制资源。
7. UI 风格定稿阶段必须独立于玩法和结构阶段推进，未完成目标参考图拆解前，不继续追加主观 UI 抛光任务。

## 阶段 21-75 后续操作列表

### 阶段 67：主策划接收《星蚀幸存者》成品描述并重编路线

- 角色：总策划 / 制作人 Agent。
- 阶段目标：把用户给出的《星蚀幸存者》成品描述转化为当前项目的有效产品合同、阶段判断、后续阶段边界和下一阶段开发合同。
- 玩家可感知效果：本阶段不直接改变游戏内体验；它为后续开发明确“星蚀污染遗迹、星辉猎人、轻幻想 Bullet Heaven、5-10 分钟 Boss 闭环、4 武器、6+ 被动、5+ 敌人、演示截图潜力”的成品方向。
- 玩法设计：保留当前已完成核心闭环，不重做移动、自动攻击、升级、结算、Boss 胜利和四武器基础。
- UI/特效/音效要求：本阶段只规划，不新增 UI、特效或音效资源。
- 资源/配置要求：本阶段只规划，不修改 Luban 表、Prefab、Sprite、Atlas 或场景。
- 技术实现边界：只编辑有效项目文档，优先 `Doc/类吸血鬼幸存者阶段开发路线.md`，必要时同步 `Doc/开发日志.md`；不修改玩法代码。
- 禁止越界内容：不直接实现敌人外观、被动扩展、刷怪节奏、Boss 技能、截图系统或局外成长。
- 自动化测试点：文档阶段无需新增 Play Mode 测试；必须执行 `git diff --check` 覆盖本次文档变更。
- Unity MCP Play Mode 验收点：本阶段无运行时代码改动，可复用上一阶段 Unity MCP 状态；如后续提交前需要复核，读取 `editor/state` 和控制台即可。
- 阶段通过标准：路线文档明确产品目标、当前阶段判断、阶段 68 下一步边界、阶段 67-75 后续规划，并写入开发日志。

### 阶段 68：敌人定位与外观第一轮补齐

- 角色：主开发 Agent。
- 阶段目标：围绕《星蚀幸存者》的星蚀污染遗迹主题，把敌人从“能刷出来”推进到“玩家一眼能区分压力来源”。
- 玩家可感知效果：普通小怪、快速敌人、高生命敌人、精英敌人和 Boss 在局内视觉上更容易区分，敌潮压力更符合“星光变成怪物、遗迹涌出污染生物”的主题。
- 玩法设计：优先复核现有敌人是否已覆盖五类定位；必要时只补最少配置或表现映射，不新增复杂 AI、不新增技能树、不新增远程敌人系统。
- 数值设计：保留当前 0-10 分钟刷怪节奏和 Boss 节点；如需微调，只允许围绕敌人定位做小幅速度、生命或权重调整。
- UI/特效/音效要求：不改 HUD 布局；如新增敌人表现，应保持低遮挡和截图可读性。
- 资源/配置要求：优先复用已有 CC0 或项目自制资源；新增资源必须写入 `Doc/视觉资产来源与许可清单.md`。
- 技术实现边界：保留 `RoguelikeSpawnDirector`、对象池和当前舞台视图入口；不重写刷怪系统。
- 禁止越界内容：不加入复杂寻路、Boss 技能、随机地图、付费资源、第二套配置系统或大型美术包整包导入。
- 自动化测试点：覆盖敌人配置可读、类型映射可用、Boss/精英仍能进入刷怪池、核心战斗闭环不破。
- Unity MCP Play Mode 验收点：运行阶段 68 指定 PlayMode 测试和全量 `GameLogic.PlayModeTests`；控制台 0 Error；必要时截图复核敌人可读性。
- 阶段通过标准：敌人五类定位有明确工程映射、文档记录、测试覆盖和 Unity MCP 验收。

### 阶段 69：被动定位与构筑差异第一轮

- 阶段目标：按成品描述补齐至少 6 种以上被动定位，让玩家能围绕攻击、移动、拾取、生命 / 恢复、暴击和金币收益做构筑判断。
- 边界：优先复用现有 `RoguelikeRewardDirector` 和配置效果解释层；不做复杂词条、稀有度、装备、背包或抽卡。
- 验收重点：被动可被三选一抽中、效果生效、HUD 汇总可读、重复获取表现稳定、Play Mode 回归通过。

### 阶段 70：5-10 分钟局内节奏与 Boss 目标复核

- 阶段目标：按“前 30 秒熟悉、1-3 分钟构筑成型、3-6 分钟敌潮上升、6-10 分钟高压与 Boss”重新复核刷怪节奏、升级速度和 Boss 目标。
- 边界：优先调整配置和测试口径，不新增新系统、不改地图结构、不做复杂 Boss 技能。
- 验收重点：固定种子模拟能观测升级次数、敌人数量、Boss 出现时间、胜利目标和控制台状态。

### 阶段 71：拾取与金币体验职责拆分

- 阶段目标：延续阶段 64-66 的结构收口，把拾取吸附、收集入账、金币补给反馈等单一边界从 `RoguelikeGame` 中拆出。
- 边界：不改拾取数值、不改金币经济、不改奖励池，只迁移运行时解释职责并补测试。
- 验收重点：旧私有边界兼容、拾取吸附与金币入账不回归、Play Mode 通过。

### 阶段 72：Boss 出场与胜利反馈第二轮

- 阶段目标：强化 Boss 目标感、出场提示、震屏 / 音效 / 视觉反馈和胜利结算表现，让 Boss 从“配置节点”更接近“本局目标”。
- 边界：早期 Boss 不做复杂技能；只处理提示、表现、奖励和胜利反馈。
- 验收重点：Boss 出场提示清晰、击败后胜利结算稳定、控制台 0 Error。

### 阶段 73：演示截图固定分辨率输出

- 阶段目标：建立用于 Steam 早期展示图的固定分辨率截图输出和取景清单，让演示图可复现。
- 边界：不新增玩法；只处理截图模式、构图入口、输出路径和文档记录。
- 验收重点：Unity MCP 截图输出可复现，文件落在 `Doc/演示截图` 或约定目录，截图说明写入文档。

### 阶段 74：局外成长第二轮最小扩展

- 阶段目标：在已有永久金币攻击加成基础上，评估并实现一个最小局外成长方向，例如初始生命或金币收益提升。
- 边界：不做装备、背包、抽卡、角色养成、商业化入口或复杂升级树。
- 验收重点：结算资源入账、下一局成长生效、UI 显示清晰、Play Mode 回归通过。

### 阶段 75：星蚀主题演示版验收包

- 阶段目标：汇总阶段 68-74 的主题、内容、截图、验证结果和下一轮边界，形成《星蚀幸存者》新一轮可演示版本说明。
- 边界：只做验收、整理和文档，不临时堆新功能。
- 验收重点：`dotnet build`、`git diff --check`、Unity MCP Play Mode、控制台 0 Error、演示说明和截图清单齐备。

### 阶段 21：敌人与战斗内容第二轮扩展

- 操作：在 `roguelike_enemy.xlsx` 中新增 2-3 个差异化普通敌人和 1 个精英敌人。
- 操作：补齐敌人生命、攻击、速度、权重和显示名，运行时继续复用 `RoguelikeContentCatalog` 与现有刷怪逻辑。
- 操作：新增 Play Mode 测试覆盖新增敌人可被配置读取、可进入刷怪池、核心战斗闭环不破。
- 实现记录：已新增普通敌人“星尘游灵”“蘑菇守卫”“晶壳虫”和精英敌人“月影骑士”。
- 实现记录：已将新增普通敌人接入阶段 1-3 普通刷怪池，并将“月影骑士”接入阶段 2-3 精英敌人配置。
- 验收：配置生成、`dotnet build`、`git diff --check`、Unity Play Mode 均通过，控制台 0 Error。

### 阶段 22：刷怪节奏与时间目标细化

- 操作：调整 `roguelike_spawn_stage.xlsx` 的 0-10 分钟阶段边界、普通怪数量、精英节点和 Boss 节点。
- 操作：补充运行时对阶段切换、Boss 节点和回退逻辑的测试。
- 操作：记录一轮 Play Mode 冒烟数据，包括存活时间、刷怪数量和 Boss 出现时间。
- 实现记录：刷怪阶段调整为 0 秒、60 秒、180 秒、360 秒、540 秒五段，普通刷怪数量从 1 逐步提升到 5。
- 实现记录：精英节点从 120 秒开始进入节奏，后期 540 秒持续承压；Boss 节点固定在 600 秒。
- 实现记录：运行时 Boss 节点改为到点后只生成一次，避免 10 分钟后重复刷 Boss。
- 验收：配置生成、`dotnet build`、`git diff --check`、Unity Play Mode 均通过，阶段切换稳定，新增节奏不破坏开始、升级、死亡、结算和重开。

### 阶段 23：武器配置与数值平衡第一轮

- 操作：复核追踪魔弹、旋刃、穿透飞镖的基础伤害、冷却、射程和升级收益。
- 操作：优先通过 `roguelike_weapon.xlsx` 调整数值，代码侧只保留解释逻辑。
- 操作：新增或扩展测试覆盖三把武器升级后输出差异。
- 实现记录：追踪魔弹调整为稳定单体输出，基础伤害 13、基础间隔 0.52、射程 5.8。
- 实现记录：旋刃调整为近身群体输出，基础伤害 8、基础间隔 0.78、射程 3.0。
- 实现记录：穿透飞镖调整为远程穿透输出，基础伤害 11、伤害成长 4、基础间隔 0.82、射程 7.8。
- 验收：三把武器均可被抽中、升级并在同局内同时生效，配置生成、`dotnet build`、`git diff --check`、Unity Play Mode 均通过。

### 阶段 24：第四武器最小实现

- 操作：新增 1 把低复杂度武器，优先选择近身脉冲或弧形散射等复用现有表现的方案。
- 操作：接入武器配置、升级池、HUD 汇总和舞台视图颜色/形状。
- 操作：补充武器解锁、升级和发射行为测试。
- 实现记录：新增“星环脉冲”，定位为短程环形脉冲武器，按冷却向周围释放多道短程投射物。
- 实现记录：星环脉冲接入 `roguelike_weapon.xlsx`、升级三选一、武器汇总、投射物颜色/形状和对象池复用链路。
- 验收：不引入武器进化、稀有度或第二套投射物框架，配置生成、`dotnet build`、`git diff --check`、Unity Play Mode 均通过。

### 阶段 25：被动道具第二轮扩展

- 操作：新增 2-3 个只影响现有数值的被动，例如冷却缩减、经验获取、最大生命。
- 操作：扩展 `EEffectType` 和解释层时保持最小范围，保留配置不可用回退。
- 操作：补充被动重复获得、HUD 显示和属性生效测试。
- 实现记录：新增“生息符文”“鹰眼纹章”“星辉轻靴”三个被动道具，分别影响最大生命与恢复、攻击与暴击、移动速度与拾取吸附范围。
- 实现记录：本阶段未新增冷却缩减或经验获取效果，优先复用现有 `AddMaxHealth`、`Heal`、`AddAttack`、`AddCritChance`、`AddMoveSpeed` 和 `AddPickupRadius`，避免提前扩展武器冷却解释逻辑。
- 实现记录：新增 `RoguelikeStage25PassiveExpansionTests`，覆盖 Luban bytes 读取、被动选择池、属性生效、重复叠加和 HUD 汇总显示。
- 验收：配置生成、`dotnet build`、`git diff --check`、Unity Play Mode 39/39 均通过，控制台 0 Error。
- 验收：被动选择不依赖 UI 脚本硬编码规则。

### 阶段 26：掉落与金币用途最小闭环

- 操作：明确金币在局内商店、结算或局外成长中的一个最小用途。
- 操作：补齐金币消耗、余额显示和失败提示。
- 操作：新增测试覆盖金币获得、消费和余额不足分支。
- 实现记录：金币掉落继续由敌人击杀概率产出，拾取后进入 `CurrentRun.Gold`，可作为局内升级三选一的消费货币。
- 实现记录：`ChooseReward` 现在会检查 `Cost/CanAfford`，金币不足时保留 `RewardChoice` 并显示失败提示，金币足够时先扣费再应用效果。
- 实现记录：新增付费选项“战地补给”，花费 8 金币立即恢复 35 生命，形成最小局内消费闭环。
- 实现记录：结算仍将未消费的局内金币一次性入账永久金币，`_metaSaved` 防止重复入账。
- 验收：新增 `RoguelikeStage26GoldLoopTests` 覆盖金币拾取、消费、余额不足和结算入账；`dotnet build`、`git diff --check`、Unity Play Mode 43/43 均通过，控制台 0 Error。
- 验收：金币不再只是收集数字，且不引入复杂经济系统。

### 阶段 27：局内目标与胜利条件扩展

- 操作：固化一条最小胜利路线，例如存活到指定时间或击败 Boss。
- 操作：补齐 `Victory` 结算文案、UI 显示和重开流程。
- 操作：新增测试覆盖胜利、失败和重开后状态归零。
- 实现记录：运行时敌人新增配置 ID 与 Boss 标记，配置表 Boss 敌人生成后会保留身份，用于判断胜利目标。
- 实现记录：Boss 被击败时进入 `Victory`，停止当帧后续战斗更新，显示“击败地牢之心，生存目标达成。”并将局内金币结算入账。
- 实现记录：普通敌人击杀仍按原逻辑生成经验/金币掉落，不触发胜利。
- 实现记录：新增 `RoguelikeStage27VictoryConditionTests`，覆盖 Boss 击杀胜利、普通击杀不胜利和胜利后重开归零。
- 验收：`dotnet build`、`git diff --check`、Unity Play Mode 46/46 均通过，控制台 0 Error。
- 验收：单局有明确结束目标，而不是只依赖死亡结束。

### 阶段 28：Boss 战第一轮可玩化

- 操作：强化 Boss 基础数值、体型/颜色表现、出现提示和击败奖励。
- 操作：只做一个 Boss 节奏，不新增复杂技能树或多阶段 Boss 框架。
- 操作：补充 Boss 出现、受击、击败和结算测试。
- 实现记录：Boss 生成时写入“地牢之心出现”提示并触发更强相机震动，让 600 秒 Boss 节点可被玩家明确感知。
- 实现记录：舞台视图会根据 `IsBoss` 将 Boss 显示为更大的紫色敌人，普通敌人仍保留原红色表现和受击闪白。
- 实现记录：击败 Boss 后额外获得 25 金币，再进入 `Victory` 并执行一次性结算入账。
- 实现记录：新增 `RoguelikeStage28BossPlayableTests`，覆盖 Boss 出场身份与提示、Boss 视图表现差异、Boss 击败奖励与胜利结算。
- 验收：`dotnet build`、`git diff --check`、Unity Play Mode 49/49 均通过，控制台 0 Error。
- 验收：Boss 战能被玩家明显感知，且不破坏普通刷怪节奏。

### 阶段 29：地图空间体验第一轮

- 操作：调优地图尺寸、边界表现或加入少量简单障碍。
- 操作：障碍仅使用简单碰撞和当前移动逻辑，不引入复杂地图生成。
- 操作：补充玩家边界、敌人追踪和投射物行为测试。
- 实现记录：运行时新增 3 个固定 `RoguelikeArenaObstacle`，用于形成轻量绕行空间，不新增随机地图或寻路系统。
- 实现记录：玩家同步位置与运行时移动会被推出障碍，敌人追踪同样受障碍阻挡，投射物命中障碍后回收。
- 实现记录：舞台视图绘制障碍块与高光，让固定地图从纯地砖变为有空间层次的场地。
- 实现记录：新增 `RoguelikeStage29MapSpaceTests`，覆盖玩家/敌人障碍阻挡、投射物障碍回收和障碍视图绘制。
- 验收：`dotnet build`、`git diff --check`、Unity Play Mode 52/52 均通过，控制台 0 Error。
- 验收：地图空间更有变化，但仍保持固定场地。

### 阶段 30：HUD 信息密度与二次元轻幻想表现打磨

- 操作：整理生命、经验、等级、时间、击杀、武器和被动的显示优先级。
- 操作：以“二次元轻幻想 Bullet Heaven UI”为目标，采用左上角色头像、生命/经验/等级，中上生存时间与敌人压力，右上武器/被动图标槽，底部低遮挡操作提示的结构。
- 操作：优先使用免费可商用字体、基础 UI 资源和 GPT 绘画产出的头像、图标或占位资源，所有外部资源必须记录来源、许可和用途。
- 操作：修复可能的文字裁切、层级遮挡和状态延迟刷新。
- 操作：保留 `GameModule.UI.ShowUIAsync<T>()` 与当前 UIWindow 体系。
- 实现记录：`BattleHudUI` 从顶部整条信息栏调整为左上角色状态面板、中上时间压力面板、右上构筑槽面板和底部低遮挡提示面板。
- 实现记录：角色状态面板展示头像占位、生命条、经验条、等级、生命、攻击、局内金币和永久金币；构筑面板展示武器/被动图标槽与构筑摘要。
- 实现记录：新增 `RoguelikeStage30HudPolishTests`，覆盖 HUD 分区锚点、角色/压力/构筑文本刷新和武器/被动图标槽生成。
- 验收：`dotnet build`、`git diff --check`、Unity Play Mode 55/55 均通过，最终控制台 0 Error。

### 阶段 31：升级三选一卡牌体验打磨

- 操作：优化升级选项池权重、重复出现规则和已满级武器过滤。
- 操作：将升级选择从基础按钮升级为卡牌式奖励界面，每张卡包含图标、标题、效果描述、价格或条件、可选和不可选状态。
- 操作：增加选择反馈、价格提示、金币不足或不可选状态提示。
- 操作：卡牌视觉优先体现二次元轻幻想风格，适合 Steam 截图展示，但不提前引入复杂稀有度、词条或武器进化系统。
- 操作：补充选项池生成和选择后状态恢复测试。
- 实现记录：`BattleChoiceUI` 从基础按钮升级为三张奖励卡，包含图标占位、标题、描述、价格和状态提示。
- 实现记录：金币不足时付费卡显示“金币不足”并不可点击，金币足够时点击后继续复用 `RoguelikeGame.ChooseReward` 恢复战斗。
- 实现记录：`RoguelikeSurvivalWeapon` 增加当前阶段 5 级上限，满级武器升级项不再进入奖励池。
- 实现记录：新增 `RoguelikeStage31RewardCardTests`，覆盖卡牌结构、价格/不可选状态、选择后恢复运行和满级武器过滤。
- 验收：未引入稀有度、词条或复杂构筑系统；`dotnet build`、`git diff --check`、Unity Play Mode 59/59 均通过，最终控制台 0 Error。

### 阶段 32：结算与局外成长最小实现

- 操作：将单局金币或成绩接入一个最小局外成长项。
- 操作：使用现有存档方式保存简单数值，避免新增账号、背包或复杂存档系统。
- 操作：结算界面区分胜利/失败标题、数据区、收益区、永久成长进度和重新开始入口，允许预留角色立绘或剪影区域。
- 操作：补充结算入账、重开保留和异常回退测试。
- 实现记录：继续使用现有永久金币 `PlayerPrefs` 存档；每 100 永久金币提供下局初始攻击 +1。
- 实现记录：结算界面已拆分为角色剪影、胜败标题、战斗数据、收益区、永久成长进度条、成长说明和重新开始入口。
- 实现记录：新增 `RoguelikeStage32SettlementGrowthTests` 覆盖入账一次、重开加成和结算 UI 展示。
- 验收：`dotnet build`、`git diff --check` 和 Unity Play Mode `GameLogic.PlayModeTests` 62/62 通过，最终控制台 0 Error。

### 阶段 33：正式音效替换与混音规则

- 操作：替换当前占位 WAV，整理命中、拾取、升级、Boss 和 UI 音效地址。
- 操作：通过 TEngine `IAudioModule` 播放，补齐音量和短冷却规则。
- 操作：补充资源存在性和安全播放测试。
- 实现记录：新增原创短音效 `Roguelike_Boss.wav` 和 `Roguelike_UIConfirm.wav`，当前五类音效均有资源地址。
- 实现记录：音效路径、音量和冷却整理为 `RoguelikeGame` 常量；运行时仍通过 `GameModule.Audio.Play(AudioType.Sound, ...)` 播放。
- 实现记录：音效冷却从全局单冷却改为按音效地址独立冷却，避免命中、拾取、升级、Boss 和 UI 确认音互相阻塞。
- 实现记录：新增 `RoguelikeStage33AudioMixTests` 覆盖资源存在、音量/冷却范围、UI 确认音和 Boss 音效互不阻塞。
- 验收：`dotnet build`、`git diff --check` 和 Unity Play Mode `GameLogic.PlayModeTests` 66/66 通过，最终控制台 0 Error。

### 阶段 34：命中特效与拾取特效 Prefab 接入

- 操作：为命中、击杀、拾取至少接入一组轻量 Prefab 表现。
- 操作：复用 TEngine 资源模块和当前舞台视图生命周期。
- 操作：补充特效生成、回收和配置缺失回退测试。
- 验收：不新增第二套特效框架。
- 实现记录：新增 `Roguelike_HitEffect.prefab`、`Roguelike_KillEffect.prefab`、`Roguelike_PickupEffect.prefab` 和自制白色像素 Sprite，均放入 `Assets/AssetRaw/Effects`，继续使用现有 `AddressByFileName` 资源收集规则。
- 实现记录：`RoguelikeGame` 新增命中、击杀、拾取三类 `RoguelikeEffectCue`，由真实战斗事件驱动表现层，不让舞台视图猜测对象删除原因。
- 实现记录：`RoguelikeBattleStageView` 增加 Prefab 优先、Sprite 回退的轻量特效池；资源模块不可用或地址缺失时不影响战斗流程。
- 实现记录：新增 `RoguelikeStage34EffectPrefabTests`，覆盖资源存在、三类 cue 生成、舞台特效生成/回收和缺失资源回退。
- 验收：`dotnet build UnityProject\UnityProject.sln --no-restore`、`git diff --check` 和 Unity Play Mode `GameLogic.PlayModeTests` 69/69 均通过，最终控制台 0 Error。

### 阶段 35：敌人和投射物 Prefab 化第一轮

- 操作：选择 1-2 个敌人和 1-2 个投射物迁移到正式 Prefab 表现。
- 操作：资源地址优先从配置或统一映射读取，保留运行时 Sprite 回退。
- 操作：补充资源加载失败、对象池复用和表现同步测试。
- 验收：不改启动、热更和资源基础流程。
- 实现记录：新增 `Roguelike_Enemy_Common.prefab`、`Roguelike_Enemy_Boss.prefab`、`Roguelike_Projectile_MagicBolt.prefab` 和 `Roguelike_Projectile_PiercingDart.prefab`，均放入 `Assets/AssetRaw/Actor` 并继续使用现有 `AddressByFileName` 收集规则。
- 实现记录：`RoguelikeBattleStageView` 对普通敌人、Boss、魔弹和穿透飞镖使用 Prefab 地址优先加载；资源模块不可用、地址缺失或非本轮武器类型时自动回退到原运行时 Sprite 表现。
- 实现记录：敌人和投射物视图继续复用原有舞台池，同步位置、旋转、体型、颜色、受击闪白和 Boss 体型差异，不改启动、热更和资源基础流程。
- 验收记录：新增 `RoguelikeStage35PrefabPresentationTests` 覆盖资源存在、缺失地址回退、表现同步和视图池复用；`dotnet build`、`git diff --check` 和全量 Unity Play Mode 均通过。

### 阶段 36：配置校验与生成流程加固

- 操作：为敌人、武器、被动、刷怪阶段增加最小配置完整性校验。
- 操作：补充缺失 ID、重复 ID、空奖励池和缺失资源地址的错误提示。
- 操作：更新 Luban 生成与验证说明到现有有效文档。
- 验收：配置错误能尽早暴露，不靠运行时空引用暴露。
- 实现记录：新增 `RoguelikeConfigValidator`，覆盖敌人、武器、奖励选项、被动遗物和刷怪阶段的关键字段、重复 ID、引用关系、空奖励池和必要资源地址。
- 实现记录：`RoguelikeGame` 首次拿到 `Tables` 后会执行一次配置校验，保存 `ConfigValidationIssues`，有问题时通过 TEngine `Log.Error` 输出中文摘要，配置不可用时仍保留原有回退。
- 实现记录：新增 `RoguelikeStage36ConfigValidationTests`，覆盖真实 Luban bytes 零问题、必要音效/特效/Actor Prefab 资源存在、缺失资源地址提示和运行时校验结果缓存。
- 验收记录：`dotnet build`、`git diff --check`、阶段 36 单测和全量 Unity Play Mode `GameLogic.PlayModeTests` 76/76 均通过，最终控制台 0 Error。

### 阶段 37：性能基线与 GC 观测

- 操作：建立一套敌人数量、投射物数量、对象池数量、GC 和帧率的观测口径。
- 操作：优先使用 Unity Profiler、MCP 可读数据或轻量运行时统计。
- 操作：记录基线数据，不先做大规模优化。
- 验收：形成可复测的性能基线和后续优化入口。
- 实现记录：新增 `RoguelikePerformanceSnapshot` 与 `RoguelikeMemoryPoolSnapshot`，记录敌人、投射物、掉落物、特效 cue、活动特效视图、池化视图、视图复用、敌人/投射物对象池、托管内存和平均帧率。
- 实现记录：`RoguelikeGame` 新增 `CapturePerformanceSnapshot` 与 `RunPerformanceBaselineSimulation`，可用固定步进复现同一口径的局内基线，不直接做优化。
- 基线记录：固定种子 `37037`、40 步、每步 `0.1` 秒，采样结果为“时间 4.0s，敌人 3，投射物 0，掉落 0，特效 1，活动特效视图 0，池化视图 0，视图复用 0，敌人池 3/0，投射物池 0/1，托管内存 27512.0KB，平均帧率 10.0”。
- 验收记录：新增 `RoguelikeStage37PerformanceBaselineTests` 覆盖对象数快照、固定步进基线和舞台视图池计数；`dotnet build`、`git diff --check` 和全量 Unity Play Mode `GameLogic.PlayModeTests` 79/79 均通过。

### 阶段 38：对象池与大量敌人压测

- 操作：在当前对象池基础上提高刷怪密度进行压测。
- 操作：检查敌人、掉落物、投射物和临时视图的归还与复用。
- 操作：补充高密度情况下的 Play Mode 测试或冒烟验证。
- 验收：无明显对象泄漏、重复释放或视图残留。
- 实现记录：`RoguelikeGame` 新增 `DebugPopulateDenseRuntimeObjects`，仅作为调试/测试入口生成高密度敌人、掉落物和投射物，仍复用现有 `MemoryPool` 与运行时对象类型。
- 实现记录：阶段 37 性能快照补充掉落物对象池计数，便于压测时同时观察敌人、投射物和掉落物池。
- 压测记录：单测覆盖 96 敌人、48 掉落物、120 投射物重开归还对象池；72/36/96 舞台视图入池后，以 24/12/32 再次生成并复用；调试入口上限钳制为 160 敌人、160 掉落物、240 投射物。
- 验收记录：新增 `RoguelikeStage38PoolingStressTests`，阶段 38 单测 3/3 通过；全量 Unity Play Mode `GameLogic.PlayModeTests` 82/82 通过，最终控制台 0 Error。

### 阶段 39：新手引导与操作提示最小实现

- 操作：增加开局移动提示、自动攻击说明、升级说明和结算提示。
- 操作：复用当前 UI 体系，优先做轻量提示，不新增复杂教程状态机。
- 操作：补充提示显示、隐藏和重开重置测试。
- 验收：第一次游玩能理解核心操作和目标。
- 实现记录：`RoguelikeGame` 新增只读 `OperationHint`，按运行、暂停、升级选择、胜利和失败阶段输出当前操作提示，不新增独立教程流程或阻塞式引导。
- 实现记录：`BattleHudUI`、`BattleControlUI`、`BattleChoiceUI` 和 `BattleSettlementUI` 统一读取 `OperationHint`，分别覆盖开局移动/自动攻击/经验拾取、升级卡牌选择、金币不足和结算后重开成长提示。
- 实现记录：`BattleControlUI` 将操作提示刷新收敛到 `RefreshControlInfo`，确保创建刷新和逐帧更新都能显示最新提示。
- 验收记录：新增 `RoguelikeStage39TutorialHintTests`，阶段 39 单测 4/4 通过；全量 Unity Play Mode `GameLogic.PlayModeTests` 86/86 通过，最终控制台 0 Error。

### 阶段 40：版本验收与演示包准备

- 操作：汇总阶段 21-39 的功能、测试、已知问题和演示路径。
- 操作：执行完整构建、Play Mode、控制台和手动冒烟验收。
- 操作：必要时整理一份中文演示说明，收敛到现有文档索引。
- 操作：准备至少 3 张可用于 Steam 商店页讨论的截图视角：高密度战斗、升级奖励三选一、结算成长界面。
- 验收：截图中体现“二次元轻幻想 Bullet Heaven UI”：角色感、卡牌奖励、图标化构筑、低遮挡 HUD。
- 验收：避免 UI 呈现廉价手游感，包括满屏按钮、过度红点、礼包式入口和遮挡战斗主体的大面板。
- 验收：形成一个可演示版本，并明确下一轮阶段边界。
- 实现记录：新增 `Doc/演示版本说明.md`，汇总当前可演示能力、推荐演示路径、截图清单、验收结论、已知不足和下一轮建议边界，并纳入 `Doc/文档索引.md`。
- 实现记录：`Doc/2D肉鸽最小可玩版说明.md` 已更新到阶段 40 完成后的能力边界，补充第四武器、Boss 胜利目标、结算成长、新手提示、配置校验、性能基线和对象池压测。
- 截图记录：阶段 40 截图输出到 `Doc/演示截图`，包含高密度战斗、升级奖励三选一和结算成长界面三类视角，用于后续 Steam 商店页方向讨论。
- 验收记录：`dotnet build UnityProject\UnityProject.sln --no-restore`、`git diff --check` 和全量 Unity Play Mode `GameLogic.PlayModeTests` 86/86 均通过，最终控制台 0 Error。

### 阶段 41：伤害数字与暴击反馈表现

- 操作：在命中表现事件中携带实际伤害和暴击标记，避免舞台视图猜测伤害来源。
- 操作：在舞台视图中生成可回收的世界空间飘字伤害数字，普通命中和暴击使用不同尺寸、颜色和后缀。
- 操作：微调命中和击杀特效持续时间与尺寸，提升高密度战斗中的可读性。
- 验收：命中 cue 能记录正数伤害；普通命中默认非暴击；舞台视图能生成并回收伤害数字；全量 Play Mode 不回归。
- 实现记录：新增 `RoguelikeDamageRoll`，投射物现在保留最终伤害和暴击标记；追踪魔弹、旋刃、穿透飞镖和星环脉冲均按单发投射物独立结算暴击。
- 实现记录：`RoguelikeEffectCue` 新增 `Damage` 与 `IsCritical`，`RoguelikeGame` 在命中时把实际伤害写入 cue，并在暴击时输出“暴击”战斗提示。
- 实现记录：`RoguelikeBattleStageView` 新增可回收 `RoguelikeDamageNumberView`，命中时生成上浮淡出的伤害飘字；暴击飘字更大、更亮并带 `!` 后缀。
- 验收记录：新增并扩展 `RoguelikeStage34EffectPrefabTests`；阶段 34 表现测试 5/5 通过；全量 Unity Play Mode `GameLogic.PlayModeTests` 90/90 通过。

### 阶段 42：视觉资产来源与许可清单

- 操作：补齐当前关键视觉资产的来源、作者、许可和用途清单。
- 操作：将 HUD、奖励、结算和操作提示仍引用的旧 Battle 图集滑条/光带替换为肉鸽专用原创 UI 资源。
- 操作：补充测试，确保肉鸽专用滑条和光带资源存在，并且当前关键视觉资产已被许可清单覆盖。
- 验收：后续导入外部免费资源前必须先更新清单；禁止来源不明、禁止商用或许可不清的资源进入 `Assets/AssetRaw`。
- 实现记录：新增 `Doc/视觉资产来源与许可清单.md`，记录当前 UI、特效、敌人/投射物 Prefab 的来源、作者、许可和用途，并纳入 `Doc/文档索引.md`。
- 实现记录：新增 `Roguelike_UI_SliderFrame`、`Roguelike_UI_SliderFill_Red`、`Roguelike_UI_SliderFill_Blue`、`Roguelike_UI_SliderFill_Yellow` 和 `Roguelike_UI_LightBand`，统一放入 `Assets/AssetRaw/UIRaw/Atlas/Roguelike`。
- 实现记录：`RoguelikeUIFactory` 的滑条和高光常量切换为肉鸽专用资源名，保留 TEngine 资源模块优先、Editor 资产回退的加载路径。
- 验收记录：新增 `RoguelikeStage42VisualAssetLicenseTests`，覆盖肉鸽 UI 不再依赖 Battle 图集关键资源、专用 UI 资源存在、许可清单覆盖关键资产和许可字段。

### 阶段 43：奖励卡图标免费资源替换第一轮

- 操作：精选导入少量明确免费可商用的通用游戏图标，优先覆盖升级奖励卡中武器、被动和付费补给的可见图标。
- 操作：更新视觉资产许可清单，记录具体包名、作者、来源 URL、许可链接、原始文件名和用途。
- 操作：奖励卡按奖励 ID 映射具体图标，保留 HUD 构筑槽默认类别图标，不在本阶段改角色立绘、字体或资源管线。
- 验收：导入资源必须位于 `Assets/AssetRaw/UIRaw/Atlas/Roguelike`，可由当前 `RoguelikeUIFactory` 资源加载路径读取，并通过 Play Mode 测试验证映射。
- 实现记录：从 Kenney Game Icons 精选导入 `star.png`、`target.png`、`right.png`、`contrast.png`、`medal1.png`、`trophy.png`、`plus.png` 和 `shoppingCart.png`，重命名为肉鸽专用图标资源。
- 实现记录：`RoguelikeUIFactory` 新增奖励图标常量与 `ResolveRewardIconSprite`，`BattleChoiceUI` 按武器、被动和付费补给 ID 使用专用图标。
- 实现记录：新增 `RoguelikeStage43KenneyIconAssetTests`，覆盖 Kenney 图标资源存在、奖励 ID 映射和许可清单覆盖。

### 阶段 44：暴击特效分层与反馈强化

- 操作：在现有命中表现 cue 基础上拆出暴击专用表现层，让暴击既保留伤害数字，也额外叠加更醒目的视觉特效。
- 操作：舞台视图中为暴击 cue 提供更大、更亮、持续时间更长的运行时 fallback 特效，并增强暴击飘字文案和尺寸。
- 操作：保持 `Hit` cue 独占伤害数字生成，避免一次暴击生成两组伤害飘字。
- 验收：暴击投射物命中会产生 `Hit` 与 `Critical` 两个 cue；舞台视图生成 2 个特效但只有 1 个伤害数字；全量 Play Mode 不回归。
- 实现记录：`RoguelikeEffectCueType` 新增 `Critical`；`RoguelikeGame` 在暴击命中时额外追加 `Critical` cue，并把暴击震屏从 `0.08f` 提升到 `0.12f`。
- 实现记录：`RoguelikeBattleStageView` 的暴击特效使用“暴击光核 + 横芒 + 竖芒”三层 Sprite fallback，名称、持续时间、基础缩放和淡出缩放均有独立参数。
- 实现记录：暴击伤害数字显示为 `暴击 {damage}!`，字号和起始缩放高于普通命中。
- 验收记录：扩展 `RoguelikeStage34EffectPrefabTests`，新增 `Stage44CriticalHitsEmitLayeredEffectCueWithoutDuplicateDamageNumber` 覆盖暴击 cue 分层、fallback 特效和伤害数字去重；相关表现测试 6/6 通过，全量 Unity Play Mode `GameLogic.PlayModeTests` 97/97 通过，最终控制台 0 Error。

### 阶段 45：Kenney 粒子贴图特效替换第一轮

- 操作：从 Kenney Particle Pack 精选导入少量 CC0 透明粒子贴图，优先覆盖命中、暴击、击杀和拾取四类既有表现节点。
- 操作：保留现有 `RoguelikeBattleStageView` 特效 cue、TEngine 资源加载、Prefab 淡出和运行时 fallback 链路，不引入第二套 VFX 框架、粒子系统或资源管线。
- 操作：新增 `Roguelike_CriticalEffect.prefab`，让阶段 44 的暴击 cue 也具备正式 Prefab 地址；原有 fallback 暴击表现继续作为资源不可用时的兜底。
- 验收：四张 Kenney 粒子贴图必须位于 `Assets/AssetRaw/Effects`；命中、暴击、击杀和拾取 Prefab 必须引用对应导入贴图 GUID；许可清单必须记录包名、作者、来源 URL、CC0 许可、原始文件名和用途。
- 实现记录：导入 `Roguelike_Kenney_HitSpark.png`、`Roguelike_Kenney_CriticalBolt.png`、`Roguelike_Kenney_KillStar.png` 和 `Roguelike_Kenney_PickupGlow.png`，分别来自 Kenney Particle Pack 的 `muzzle_03.png`、`spark_05.png`、`star_07.png` 和 `circle_05.png`。
- 实现记录：`RoguelikeBattleStageView` 新增 `CriticalEffectPrefabAddress = "Roguelike_CriticalEffect"`，并让 `RoguelikeEffectCueType.Critical` 映射到该 Prefab 地址。
- 实现记录：`Roguelike_HitEffect.prefab`、`Roguelike_KillEffect.prefab` 和 `Roguelike_PickupEffect.prefab` 已从旧自制像素 Sprite 切换为 Kenney 粒子贴图，缩放按 512 像素贴图的世界尺寸做保守下调。

### 阶段 46：展示战斗构图模式第一轮

- 操作：新增固定种子的展示战斗构图入口，专门服务 Steam/演示截图制作，避免继续使用阶段 38 的矩阵式高密度压测状态作为商店图基础。
- 操作：构图入口只生成运行时调试状态，不接入正常刷怪、升级或结算流程；继续复用当前 `RoguelikeGame` 运行时对象、`MemoryPool`、`RoguelikeBattleStageView`、伤害数字和特效 cue。
- 操作：展示状态中同时包含 Boss、精英/普通敌群、多武器弹幕、经验/金币掉落、命中、暴击、击杀和拾取反馈，形成更接近真实战斗瞬间的画面信息密度。
- 验收：展示构图必须可由固定 seed 复现；敌人位置不能呈规则矩阵；舞台视图刷新后必须能生成特效视图、飘字伤害数字、拾取脉冲和震屏反馈。
- 实现记录：`RoguelikeGame` 新增 `DebugPopulateShowcaseRuntimeObjects(int seed = 46001)`，生成 22 个非规则排布敌人、10 个掉落物、20 个投射物和 6 个表现 cue，并设置展示用时间、等级、击杀数、金币、武器汇总和操作提示文案。
- 实现记录：新增 `RoguelikeStage46ShowcaseCompositionTests`，覆盖展示构图数量、非矩阵分布、Boss/普通敌混排、四类武器、暴击投射物、命中/暴击/击杀/拾取 cue，以及舞台视图特效和伤害数字生成。

### 阶段 47：展示截图 UI 控制第一轮

- 操作：在阶段 46 展示构图入口基础上新增展示截图模式，专门服务 Steam/演示截图制作时的 UI 遮挡控制。
- 操作：截图模式隐藏底部操作提示和 HUD 提示条，保留角色状态、时间压力与构筑槽等关键信息，但降低其透明度，避免遮住战斗主体。
- 操作：结算截图模式隐藏结算面板内的操作提示文案；正常 `StartNewRun` 自动关闭截图模式，避免影响普通游玩、新手提示和交互。
- 验收：展示构图入口必须自动开启截图模式；正常开局必须恢复默认 UI；HUD、控制条和结算面板必须能随截图模式开关即时隐藏或恢复。
- 实现记录：`RoguelikeGame` 新增 `IsShowcaseScreenshotMode`、`DebugSetShowcaseScreenshotMode(bool)`、`ShouldShowOperationHintUi` 和 `CurrentHudPanelAlpha`，阶段 46 展示入口会自动开启截图模式。
- 实现记录：`BattleHudUI` 根据截图模式隐藏战斗提示面板并降低上方 HUD 面板透明度；`BattleControlUI` 隐藏底部操作提示条；`BattleSettlementUI` 在截图模式隐藏结算提示。
- 实现记录：新增 `RoguelikeStage47ShowcaseScreenshotModeTests`，覆盖截图模式默认状态、展示入口自动开启、正常开局关闭、HUD/操作提示/结算提示隐藏与恢复。

### 阶段 48：HUD 构筑槽图标正式化第一轮

- 操作：复用阶段 43 已导入并记录许可的 Kenney Game Icons，不新增外部资源包。
- 操作：HUD 右上武器槽按 `MagicBolt`、`SpinningBlade`、`PiercingDart` 和 `StarRingPulse` 显示具体武器图标，不再只显示通用武器占位图。
- 操作：HUD 右上被动槽按成长、输出和通用功能类型显示对应被动图标，空槽保留默认被动占位图。
- 验收：展示构图中的四类武器图标必须全部可辨识；被动槽必须覆盖成长、输出、通用和空槽回退；奖励卡图标与许可清单不回归。
- 实现记录：`RoguelikeUIFactory` 新增 `ResolveWeaponIconSprite` 和 `ResolveRelicIconSprite`，统一返回 HUD 构筑槽专用图标地址。
- 实现记录：`BattleHudUI` 记录武器槽和被动槽 `Image` 引用，并在刷新 HUD 时根据当前构筑状态动态应用对应 Sprite。
- 实现记录：新增 `RoguelikeStage48HudBuildIconTests`，覆盖展示构图四武器图标、被动分类图标、空槽回退和工厂映射。
- 验收记录：`dotnet build UnityProject\UnityProject.sln --no-restore` 通过，保留既有 `System.Net.Http` 和 `System.IO.Compression` 版本冲突警告；`git diff --check` 通过，仅有 Windows 换行提示。
- 验收记录：Unity MCP `editor/state` 可读取且 `ready_for_tools=true`；阶段 48 单测 3/3 通过，HUD/图标/截图模式相关回归 11/11 通过，全量 Unity Play Mode `GameLogic.PlayModeTests` 109/109 通过，最终控制台 0 Error。

### 阶段 49：伤害数字细节表现强化第二轮

- 操作：继续复用现有 `RoguelikeEffectCue` 和 `RoguelikeDamageNumberView`，不改伤害结算、暴击概率或数值配置。
- 操作：伤害飘字补充阴影层，增强浅色战斗背景和高密度对象中的可读性。
- 操作：将普通命中、高伤害非暴击和暴击飘字拆成三档视觉表现，分别调整字号、颜色、起始缩放、漂移方向和上浮距离。
- 操作：暴击飘字保留“暴击 {damage}!”文案，并延长生命周期和上浮距离，避免与普通命中混在一起。
- 验收：普通、重击和暴击飘字必须具备可测试的字号、颜色和缩放差异；暴击飘字生命周期必须长于普通命中；全量 Play Mode 不回归。
- 实现记录：`RoguelikeDamageNumberView` 新增阴影 `TextMesh`，并按普通、重击和暴击设置不同颜色、字号、缩放、漂移和淡出参数。
- 实现记录：新增 `RoguelikeStage49DamageNumberPolishTests`，覆盖普通/重击/暴击表现差异、阴影排序和暴击生命周期。
- 实现记录：特效和伤害数字刷新现在会限制单帧反馈 delta，避免 PlayMode 套件长帧让展示构图短特效生成后立即过早回收。
- 验收记录：`dotnet build UnityProject\UnityProject.sln --no-restore` 通过，保留既有 `AdditionalFile`、nullable、`System.Net.Http` 和 `System.IO.Compression` 警告；`git diff --check` 通过，仅有 Windows 换行提示。
- 验收记录：Unity MCP `editor/state` 可读取且 `ready_for_tools=true`；阶段 49 单测 2/2 通过，阶段 34/46 表现回归 8/8 通过，全量 Unity Play Mode `GameLogic.PlayModeTests` 111/111 通过，最终控制台 0 Error。

### 阶段 50：掉落物/拾取物正式表现第一轮

- 操作：继续复用当前掉落、吸附、拾取和拾取特效逻辑，不改掉落规则、金币经济、拾取半径或配置表。
- 操作：金币拾取物复用已记录的项目自制 `Roguelike_UI_GoldIcon`，经验拾取物复用阶段 43 已导入并记录许可的 Kenney CC0 `Roguelike_UI_Icon_RelicGrowth`。
- 操作：舞台视图中的掉落物从运行时纯色菱形切换为正式 Sprite，并在资源不可用时保留白色菱形回退，避免资源缺失阻塞核心闭环。
- 操作：池化拾取物复用时必须按当前类型重新应用 Sprite、旋转、颜色和尺寸，避免金币/经验图标串用。
- 验收：经验和金币掉落物必须显示可测试的正式 Sprite；缺失资源时必须回退且不破坏视图池复用；全量 Play Mode 不回归。
- 实现记录：`RoguelikeUIFactory.LoadSprite` 公开给运行时舞台表现层复用，继续沿用 TEngine 资源模块优先、Editor 资源路径回退的加载方式。
- 实现记录：`RoguelikeBattleStageView` 新增拾取物 Sprite 地址映射和测试覆盖用地址 override，经验/金币拾取物会在每次刷新时重新应用正式图标或回退表现。
- 实现记录：新增 `RoguelikeStage50PickupPresentationTests`，覆盖经验/金币正式 Sprite、缺资源回退和池化视图复用后重新应用正确 Sprite。
- 验收记录：`dotnet build UnityProject\UnityProject.sln --no-restore` 通过，保留既有 `AdditionalFile`、nullable、`System.Net.Http` 和 `System.IO.Compression` 警告；`git diff --check` 通过，仅有 Windows 换行提示。
- 验收记录：Unity MCP `editor/state` 可读取且 `ready_for_tools=true`；阶段 50 单测 2/2 通过，阶段 42/43/9/46 相关回归 10/10 通过，全量 Unity Play Mode `GameLogic.PlayModeTests` 113/113 通过，最终控制台 0 Error。

### 阶段 51：升级奖励卡面正式底纹第一轮

- 操作：继续复用当前升级三选一奖励逻辑、金币消耗、可选/不可选判断和阶段 43 已接入的正式奖励图标，不改变奖励池或选择流程。
- 操作：升级奖励卡面复用已记录许可的项目自制 `Roguelike_UI_LightBand`、`Roguelike_UI_PanelFrame`、`Roguelike_UI_GoldIcon` 和滑条填充资源，补充更正式的底纹、类别徽带、图标底座和金币徽章。
- 操作：武器、被动和付费补给卡必须有可测试的类别色带差异；金币不足或空卡位必须显示不可选遮罩，但不阻塞按钮交互状态本身的测试。
- 验收：阶段 51 Play Mode 测试必须覆盖卡面底纹层、顶部徽带、图标底座、付费金币徽章、不可选遮罩和类别色带；全量 Play Mode 不回归。
- 实现记录：`BattleChoiceUI` 为每张奖励卡新增正式底纹、类别底纹、顶部徽带、图标底座、金币徽章和不可选遮罩，并在刷新奖励时根据奖励类型、付费状态和金币是否足够动态切换表现。
- 实现记录：新增 `RoguelikeStage51RewardCardSkinTests`，覆盖正式底纹层、金币徽章显隐、不可选遮罩层级和武器/被动/不可选卡的类别色带差异。
- 验收记录：`dotnet build UnityProject\UnityProject.sln --no-restore` 通过，保留既有 `AdditionalFile`、nullable、`System.Net.Http` 和 `System.IO.Compression` 警告；`git diff --check` 通过，仅有 Windows 换行提示。
- 验收记录：Unity MCP `editor/state` 可读取且 `ready_for_tools=true`；阶段 51 单测 2/2 通过，阶段 31/43/51 相关回归 9/9 通过，全量 Unity Play Mode `GameLogic.PlayModeTests` 115/115 通过，最终控制台 0 Error。

### 阶段 52：外部免费 UI 框体与提示装饰接入第一轮

- 操作：精选导入 Kenney Fantasy UI Borders 的 CC0 面板、透明卡牌边框、按钮框和分隔装饰，不整包导入，不引入付费插件或第二套 UI 框架。
- 操作：`RoguelikeUIFactory` 将通用面板、奖励卡和按钮框切换到外部 Kenney UI Sprite，同时保留原项目自制框体常量供小型图标底座和遮罩继续复用。
- 操作：HUD 角色状态、时间压力、构筑槽、底部操作提示、升级三选一和结算界面补充外部分隔装饰，提升正式感但不改变交互流程、奖励规则或布局职责。
- 验收：阶段 52 Play Mode 测试必须覆盖外部 UI 文件存在、许可清单记录、UI 工厂常量和 HUD/操作提示/奖励/结算四类 UI 的实际 Sprite 绑定；阶段 30/31/32/39/51 相关 UI 回归与全量 Play Mode 不回归。
- 实现记录：已新增 `Roguelike_Kenney_UI_PanelStone.png`、`Roguelike_Kenney_UI_CardBorder.png`、`Roguelike_Kenney_UI_ButtonFrame.png` 和 `Roguelike_Kenney_UI_DividerFade.png`，并在 `.meta` 中为三类框体配置 12 像素九宫格边框。
- 实现记录：已新增 `RoguelikeStage52ExternalUiAssetTests`，覆盖 Kenney Fantasy UI Borders 资源、许可清单、UI 工厂常量和四类界面接入。
- 验收记录：`dotnet build UnityProject\UnityProject.sln --no-restore` 通过，保留既有 `AdditionalFile`、nullable、`System.Net.Http` 和 `System.IO.Compression` 警告；`git diff --check` 通过，仅有 Windows 换行提示。
- 验收记录：Unity MCP `editor/state` 可读取且 `ready_for_tools=true`；阶段 52 与阶段 30/31/32/39/51 UI 相关回归 21/21 通过，全量 Unity Play Mode `GameLogic.PlayModeTests` 118/118 通过，最终控制台 0 Error。

### 阶段 53：玩家与角色正式视觉表现第一轮

- 操作：优先为玩家/角色头像、结算角色区和必要的局内玩家表现补免费或项目自制正式资源，不改玩家移动、受伤、生命或结算规则。
- 操作：若使用外部资源，必须先写入 `Doc/视觉资产来源与许可清单.md`，并优先精选少量文件进入 `Assets/AssetRaw` 既有链路。
- 验收：玩家/角色资源存在、许可清单覆盖、HUD/结算/舞台表现实际绑定且全量 Play Mode 不回归。
- 实现记录：已从 Kenney Roguelike Characters 的 `Spritesheet/roguelikeChar_transparent.png` 中精选 `(1,6)` 角色格，裁切为 `Roguelike_Kenney_PlayerAdventurer.png` 并放入 `Assets/AssetRaw/Actor`，同时导出 `Roguelike_Kenney_PlayerPortrait.png` 作为 HUD 与结算头像。
- 实现记录：`RoguelikeUIFactory.HeroPortraitSprite` 已切换到 `Roguelike_Kenney_PlayerPortrait`，并保留 `OriginalHeroPortraitSprite` 指向旧自制头像以便后续回退；`LoadSprite` 增加 `Assets/AssetRaw/Actor` 编辑器回退读取路径。
- 实现记录：`RoguelikeBattleStageView` 新增 `PlayerSpriteAddress`，局内玩家创建后会绑定 Kenney 玩家 Sprite，并保留原 `Rigidbody2D`、`CircleCollider2D` 与 `RoguelikePlayerMotor` 链路。
- 实现记录：新增 `RoguelikeStage53PlayerVisualPresentationTests`，覆盖玩家角色资源存在、许可清单、HUD/结算头像绑定和局内玩家 Sprite/物理组件不变。

### 阶段 54：敌人正式视觉表现第一轮

- 操作：按普通敌人、精英敌人和 Boss 类型补齐更明确的免费或自制表现资源，继续复用当前 Prefab/对象池/刷怪配置。
- 操作：不新增敌人 AI、不改刷怪节奏、不扩 Boss 技能；本阶段只提升敌人识别度和展示图可读性。
- 验收：不同敌人类型可测试地绑定正式 Sprite 或 Prefab 表现，资源缺失仍可回退，视图池复用不串图，全量 Play Mode 不回归。

### 阶段 55：命中、升级与拾取特效表现强化第三轮

- 操作：继续复用 Kenney Particle Pack 或项目自制贴图，补强命中、升级、拾取、击杀和暴击的层次，不引入付费插件作为默认依赖。
- 操作：表现增强只挂在现有 cue、Prefab 和舞台视图链路上，不改变伤害数值、暴击概率、掉落或升级规则。
- 验收：关键反馈节点均有可测试的正式表现差异，资源缺失仍回退，全量 Play Mode 不回归。

### 阶段 56：HUD 与提示动效反馈第一轮

- 操作：为 HUD 数值刷新、操作提示、奖励卡出现/选择和按钮点击补轻量动效或节奏反馈，优先用项目内最小实现。
- 操作：Feel 等付费插件仅在明确接受授权后作为可选接入方案；若接入，必须通过轻量桥接层触发现有反馈，不让核心玩法依赖插件。
- 验收：HUD/提示动效在 Play Mode 可观察且可测试，不影响普通游玩 UI、展示截图模式和奖励/结算流程。

### 阶段 57：外部 UI 正式资产替换第二轮

- 操作：按用户选定的 `Tiny RPG - Mana Soul GUI` + `RPG UI` 方向替换当前过于简陋的外部 UI 框体，不改 HUD、奖励、结算或提示的交互流程。
- 操作：优先导入授权清晰、可直接下载的 `RPG UI` 官方附件 `rpg_ui_0.7z` 中少量关键 PNG，裁切为项目内 UI 资源；`Tiny RPG - Mana Soul GUI` 因 itch 官方 zip 下载流当前未取得最终 zip，本轮只记录选型与许可，不使用预览图代替。
- 实现记录：已新增 `Roguelike_SystemG6_UI_PanelFrame.png`、`Roguelike_SystemG6_UI_CardFrame.png`、`Roguelike_SystemG6_UI_ButtonFrame.png`、`Roguelike_SystemG6_UI_DividerMetal.png`、`Roguelike_SystemG6_UI_SliderFrame.png`、`Roguelike_SystemG6_UI_SliderFillRed.png`、`Roguelike_SystemG6_UI_SliderFillYellow.png` 和 `Roguelike_SystemG6_UI_SlotFrame.png`，并写入许可清单。
- 实现记录：`RoguelikeUIFactory` 的通用面板、卡牌、按钮、分隔线、生命条边框、生命条填充和成长进度填充已切换到 System G6 资源；Kenney 第一轮外部 UI 常量保留为历史/回退记录。
- 验收：新增 `RoguelikeStage57ExternalUiUpgradeTests` 覆盖 System G6 文件、许可清单、Tiny RPG 选型记录和 UI 工厂常量；阶段 30/31/32/39/42/52 相关 UI 回归预期同步更新。
- 验收记录：`dotnet build UnityProject\UnityProject.sln --no-restore` 通过，保留既有 `AdditionalFile`、nullable、`System.Net.Http` 和 `System.IO.Compression` 警告；`git diff --check` 通过，仅有 Windows 换行提示。
- 验收记录：Unity MCP 刷新资源时曾短暂断开并自动恢复；阶段 57 单测 4/4 通过，阶段 30/31/32/39/42/52/57 UI 回归 26/26 通过，全量 Unity Play Mode `GameLogic.PlayModeTests` 125/125 通过，清理临时 `InitTestScene` 与已知 `PlaymodeLauncher.cs:113` 退出噪声后最终控制台 0 Error。

### 阶段 58：UI 槽位正式框体绑定

- 操作：继续推进 UI 正式资产落地，把阶段 57 已导入但未绑定的 `Roguelike_SystemG6_UI_SlotFrame` 接入当前可见 UI，不新增 UI 流程或交互逻辑。
- 实现记录：`RoguelikeUIFactory` 新增 `SlotFrameSprite` 和 `CreateFramedIconSlot`，统一生成“槽框 + 内部图标”的小型 UI 控件。
- 实现记录：`BattleHudUI` 的右上武器槽与被动槽现在使用 System G6 槽框包住现有正式图标，保持阶段 48 的武器/被动图标映射不变。
- 实现记录：`BattleChoiceUI` 的奖励图标底座从旧自制面板框切换到 System G6 槽框，让奖励卡核心图标区与新 RPG UI 框体风格统一。
- 实现记录：复查 `Tiny RPG - Mana Soul GUI` 官方 itch 下载页，确认 `download_url`、上传 ID `13498579` 和页面列出的 94 kB `tinyRPG_manaSoulGUI_v_1_0.zip`；当前 `/file/13498579` 端点仍返回 `invalid key`，因此本阶段不导入 Tiny RPG，不使用预览图替代。
- 验收：新增 `RoguelikeStage58UiSlotFrameBindingTests` 覆盖 HUD 构筑槽外框与奖励卡图标底座实际绑定 System G6 槽框；阶段 30、51、57 UI 回归预期同步更新。
- 验收记录：`dotnet build UnityProject\UnityProject.sln --no-restore` 通过，保留既有 `AdditionalFile`、nullable、`System.Net.Http` 和 `System.IO.Compression` 警告。

### 阶段 59：UI 金币徽章正式框体绑定

- 操作：继续推进 UI 正式资产落地，把奖励卡和结算界面中仍然裸露的金币/成长小图标包进 System G6 正式槽框，不新增资源、不改奖励、金币或结算规则。
- 实现记录：`BattleChoiceUI` 的 `卡牌金币徽章_{n}` 现在通过 `CreateFramedIconSlot` 生成，外层 `卡牌金币徽章_{n}框` 使用 `Roguelike_SystemG6_UI_SlotFrame`，内部仍使用 `Roguelike_UI_GoldIcon`。
- 实现记录：`BattleSettlementUI` 的 `金币成长徽章` 同样改为“槽框 + 金币图标”结构，和永久成长进度条、System G6 面板框保持一致。
- 验收：新增 `RoguelikeStage59UiBadgeFrameBindingTests` 覆盖升级卡付费金币徽章与结算成长金币徽章实际绑定 System G6 槽框；阶段 32、51 UI 回归预期同步更新。

### 阶段 60：UI 提示面板正式装饰线绑定

- 操作：继续推进 UI 正式资产落地，把 HUD 底部 `战斗提示面板` 中仍偏简陋的提示条装饰补齐为 System G6 金属分隔装饰线，不新增外部资源、不修改提示文案、不改变操作提示显示条件。
- 实现记录：`BattleHudUI` 在 `战斗提示光带` 后新增 `战斗提示外部装饰线`，使用 `RoguelikeUIFactory.DividerFadeSprite`，实际绑定 `Roguelike_SystemG6_UI_DividerMetal`，位置放在提示面板底部以避免遮挡提示文字。
- 验收：新增 `RoguelikeStage60UiHintDividerBindingTests` 覆盖 `战斗提示面板` 继续使用 `Roguelike_SystemG6_UI_PanelFrame`，以及 `战斗提示外部装饰线` 实际绑定 `Roguelike_SystemG6_UI_DividerMetal`。

### 阶段 61：UI 奖励卡不可选遮罩正式框体绑定

- 操作：继续推进 UI 正式资产落地，把升级奖励卡的不可选遮罩从项目自制面板框切换到 System G6 正式卡框，不新增资源、不修改奖励池、不修改金币消耗或可选判定。
- 实现记录：`BattleChoiceUI` 的 `卡牌不可选遮罩_{n}` 现在使用 `RoguelikeUIFactory.CardFrameSprite`，实际绑定 `Roguelike_SystemG6_UI_CardFrame`；遮罩颜色、透明度和显示/隐藏逻辑保持不变。
- 验收：新增 `RoguelikeStage61RewardCardDisabledOverlayTests` 覆盖可选卡遮罩保持隐藏、金币不足卡遮罩显示，并确认显示中的遮罩绑定 System G6 正式卡框。

### 阶段 62：UI 奖励卡顶部徽带正式装饰绑定

- 操作：继续推进 UI 正式资产落地，把升级奖励卡顶部徽带从自制蓝色进度条资源切换为 System G6 金属分隔装饰，不新增资源、不修改奖励池、不修改奖励类别和可选判定。
- 实现记录：`BattleChoiceUI` 的 `卡牌顶部徽带_{n}` 现在使用 `RoguelikeUIFactory.DividerFadeSprite`，实际绑定 `Roguelike_SystemG6_UI_DividerMetal`；原有武器偏暖、被动偏蓝、金币不足淡化的颜色逻辑保持不变。
- 验收：新增 `RoguelikeStage62RewardCardRibbonTests` 覆盖三张奖励卡顶部徽带均绑定 System G6 金属分隔装饰，并保留类别着色与不可选淡化；同步更新阶段 51 卡面皮肤回归预期。

### 阶段 63：UI 奖励卡内层描边正式框体绑定

- 操作：继续推进 UI 正式资产落地，把升级奖励卡内层 `卡牌边框_{n}` 从自制光带资源切换为 System G6 正式卡框，不新增资源、不修改奖励池、不修改奖励类别和可选判定。
- 实现记录：`BattleChoiceUI` 的 `卡牌边框_{n}` 现在使用 `RoguelikeUIFactory.CardFrameSprite`，实际绑定 `Roguelike_SystemG6_UI_CardFrame`；原有透明度、卡牌布局、图标底座、顶部徽带和不可选遮罩逻辑保持不变。
- 验收：新增 `RoguelikeStage63RewardCardInnerFrameTests` 覆盖三张奖励卡内层描边均绑定 System G6 正式卡框。

### 阶段 64：RoguelikeGame 职责梳理与最小结构拆分

- 操作：梳理 `RoguelikeGame` 中单局状态、刷怪、武器、掉落、反馈、配置解释、展示调试入口和 UI 只读状态的职责边界，优先识别可拆出的纯逻辑辅助。
- 操作：只做最小结构拆分，例如运行内容目录构建、配置解释结果、刷怪阶段解析或反馈状态计算这类不依赖 Unity 对象生命周期的辅助；不引入完整 ECS、复杂服务层、第二套配置框架或新的全局管理器。
- 操作：保持 `RoguelikeGame.Instance`、当前 UI 调用、对象池、TEngine 模块访问和 Play Mode 测试入口兼容，不改变玩法数值、奖励池、刷怪表、UI 风格或美术资源。
- 实现记录：新增 `RoguelikeSpawnDirector`，负责 `GetActiveSpawnStage`、`PickEnemyConfig`、`PickWeightedEnemy`、`GetEnemyMoveSpeed` 和可重置的 Boss 已刷出状态。
- 实现记录：`RoguelikeGame` 新增 `_spawnDirector` 字段，开局时重置刷怪状态；原私有方法改为委托给 `RoguelikeSpawnDirector`，避免破坏既有阶段回归和 UI/舞台访问方式。
- 验收：新增 `RoguelikeStage64StructureSplitTests`，覆盖无需 `RoguelikeGame` 状态即可选择活跃刷怪阶段、Boss 一次性刷出与重置、阶段普通敌人加权池选择，以及 `RoguelikeGame` 旧私有边界仍能委托到新辅助类。
- 不足：本阶段只处理刷怪配置解释，武器发射、奖励池构建、拾取反馈和展示构图仍留在 `RoguelikeGame`，后续应继续按单一职责小步拆分。

### 阶段 65：奖励池构建职责拆分

- 操作：继续按阶段 64 的结构债务收口方向推进，选择“升级三选一奖励池构建”作为单一纯逻辑边界，不新增奖励内容、不改奖励数值、不改金币补给、不改 UI 风格和美术资源。
- 操作：保留 `RoguelikeGame` 对运行时属性、武器列表、配置表、被动应用和武器升级的所有权，只把候选池组装和三选一抽取迁出，避免一次性迁移战斗状态。
- 实现记录：新增 `RoguelikeRewardDirector`，负责 `BuildLevelUpOptions`、`CreateLevelUpChoicePool`、`AddConfiguredRewardChoices`、`AddWeaponChoice`、`AddPassiveChoices`、`AddConfiguredPassiveChoices` 和 `AddPassiveFromConfig`。
- 实现记录：`RoguelikeGame` 新增 `_rewardDirector` 字段和 `CreateRewardContext`，通过上下文把配置表、当前武器列表和必要运行时回调交给奖励辅助类；原私有方法保留为委托入口，兼容既有反射测试。
- 验收：新增 `RoguelikeStage65RewardDirectorTests`，覆盖无需完整 `RoguelikeGame` 状态即可构建升级奖励池、三选一抽取结果去重，以及 `RoguelikeGame` 旧私有被动边界仍能委托到新辅助类并正确应用配置被动效果。
- 不足：本阶段只处理奖励池构建，奖励选项的最终执行仍通过 `RoguelikeGame` 的运行时状态回调完成；武器发射、拾取反馈、展示构图和结算存档仍留在主类。

### 阶段 66：武器运行时解释职责拆分

- 操作：继续按阶段 64-65 的结构债务收口方向推进，选择“武器运行时解释”作为单一纯逻辑边界，不新增武器内容、不改武器数值、不改配置表、不改投射物对象池、不改 UI 风格和美术资源。
- 操作：保留 `RoguelikeGame` 对真实单局状态、投射物列表、随机数、暴击伤害滚动和对象池创建的所有权，只把武器冷却推进、目标选择、发射参数解释和四种武器发射流程迁出，避免一次性迁移战斗状态。
- 实现记录：新增 `RoguelikeWeaponDirector`，负责 `UpdateWeapons`、`FireWeapon`、`FireMagicBolt`、`FireSpinningBlade`、`FirePiercingDart`、`FireStarRingPulse`、`GetWeaponInterval`、`GetWeaponDamage`、`GetWeaponRange` 和 `GetWeaponSpeed`。
- 实现记录：`RoguelikeGame` 新增 `_weaponDirector` 字段和 `CreateWeaponContext`，通过上下文把配置表、武器列表、敌人列表、玩家位置、攻击数值、投射物创建和运行时反馈回调交给武器辅助类；原私有方法保留为委托入口，兼容既有测试和访问方式。
- 验收：新增 `RoguelikeStage66WeaponDirectorTests`，覆盖无需完整 `RoguelikeGame` 状态即可发射追踪魔弹、武器冷却推进与主武器冷却返回，以及 `RoguelikeGame` 旧私有武器边界仍能委托到新辅助类并生成投射物。
- 不足：本阶段只处理武器运行时解释；拾取吸附/收集、反馈 cue、展示构图和结算存档仍留在 `RoguelikeGame`，后续应继续按单一边界小步拆分。

### 阶段 1 实现记录

- 玩家运行时视图挂载 `Rigidbody2D`、`CircleCollider2D` 和轻量移动组件。
- 移动在 `FixedUpdate` 中通过 `Rigidbody2D.MovePosition` 执行。
- `RoguelikeGame` 继续统一保存输入、速度、地图边界和同步后的玩家位置。
- 主摄像机加入平滑跟随，并处理运行时舞台重建时的摄像机保留。
- Unity Play Mode 验收结果：移动、暂停、边界、重开归零、摄像机跟随均通过，控制台 0 错误。

### 阶段 2-3 实现记录

- 敌人运行时视图已挂载运动学 `Rigidbody2D`、触发碰撞体和物理帧同步组件。
- 保留现有四周生成、随时间增量和逻辑追踪规则。
- Unity Play Mode 验收结果：敌人持续生成并追近玩家，接触伤害生效，控制台 0 错误。

### 阶段 4 实现记录

- 原方向范围攻击已替换为最近敌人自动索敌和直线投射物。
- 投射物具备速度、伤害、剩余射程和独立运行时视图。
- Unity Play Mode 验收结果：投射物生成、命中、伤害和击杀均通过，控制台 0 错误。

### 阶段 5-6 实现记录

- 升级时进入三选一阶段并冻结局内时间，选择后恢复运行并应用属性强化。
- 时间推进会增加单次刷怪数，并提高敌人生命、攻击和速度。
- Unity Play Mode 验收结果：升级与难度曲线均通过，控制台 0 错误。

### 阶段 10/12 实现记录

- HUD、升级三选一和结算界面已完成截图复验。
- 修复公共按钮标签高度不足导致小按钮文字被完全裁切的问题。
- 开始、升级、死亡、结算和重开流程均已通过 Play Mode 验收，控制台 0 错误。

### 阶段 11 实现记录

- 复用当前运行时生成的轻量 Sprite 表现，不新增第二套特效框架。
- 敌人受击继续保留短时闪白，投射物命中和敌人击杀会触发短时相机震动。
- 经验或金币拾取会触发 `PickupFlash`，舞台视图在玩家位置显示短时拾取脉冲。
- 当前仓库 `Assets/AssetRaw/Audios` 暂无可用音频资源，因此未硬接空路径音效；后续正式音效应通过 TEngine `IAudioModule.Play(AudioType.Sound, path)` 接入资源链路。
- 已新增 `RoguelikeStage11FeedbackTests`，覆盖命中反馈、拾取反馈、舞台反馈视图和反馈计时衰减。
- Unity Play Mode 验收结果：`GameLogic.PlayModeTests` 14 项通过，控制台 0 错误。

### 阶段 7 实现记录

- 新增运行时武器状态，默认武器为“追踪魔弹”。
- 升级三选一加入“魔弹升级”“解锁旋刃”和“旋刃升级”。
- “旋刃”为第二种差异化武器，会按冷却向周围环形齐射短程刀刃。
- HUD 会显示当前武器等级，投射物按武器来源使用不同视觉颜色。
- 已新增 `GameLogic.PlayModeTests`，覆盖武器选项解锁、武器升级和多武器同时发射。
- Unity Play Mode 验收结果：`RoguelikeStage7WeaponTests` 2 项通过，控制台 0 错误。

### 阶段 8 实现记录

- 升级三选一新增“磁力核心”“聚能护符”“疾风靴”三种被动道具。
- 被动道具复用 `RoguelikeRelicTemplate` 和 `CurrentRun.Relics`，仅影响拾取吸附、投射物伤害和移动速度。
- HUD 新增被动汇总显示，重复获得同一被动时显示叠加数量。
- Unity Play Mode 验收结果：`GameLogic.PlayModeTests` 4 项通过，控制台 0 错误。

### 阶段 9 实现记录

- 敌人、掉落物和投射物实现 `IMemory`，由 TEngine `MemoryPool` 获取和释放。
- 重开、敌人死亡、投射物命中或耗尽、掉落物拾取时会归还池化运行时对象。
- 舞台视图新增敌人、掉落物和投射物 Transform 复用栈，实体消失后不再直接销毁临时视图。
- Unity 组件获取改为显式 Unity 判空，避免池化视图复用时受到 `UnityEngine.Object` 假 null 影响。
- Unity Play Mode 验收结果：`GameLogic.PlayModeTests` 6 项通过，控制台 0 错误。

### 阶段 13 实现记录

- 复用现有 Luban 生成配置：`TbRoguelikeEnemy`、`TbRoguelikeChoice` 和 `TbRoguelikeRelic`。
- 生存刷怪优先从敌人配置表读取基础生命和攻击，并按波次继续叠加运行时难度成长。
- 升级三选一会从配置表读取基础奖励效果，配置不可用时回退到原有硬编码基础选项。
- 当前表结构尚未覆盖速度、范围、攻击频率、武器和阶段 8 被动效果，这些仍保留代码定义，避免阶段 13 扩表过重。
- 已新增 `RoguelikeStage13ConfigTests`，覆盖配置二进制加载、配置选项效果应用和配置敌人刷怪。
- Unity Play Mode 验收结果：`GameLogic.PlayModeTests` 9 项通过，控制台 0 错误。

### 阶段 14 实现记录

- 复核现有地图边界、掉落物吸附和拾取逻辑，当前实现满足阶段 14 最小目标。
- 新增 `RoguelikeStage14BoundaryPickupTests`，覆盖玩家位置边界夹取、近距离经验掉落吸附和收集入账。
- 未新增地图系统、掉落表或复杂拾取反馈，避免越过阶段 14 边界。
- Unity Play Mode 验收结果：`GameLogic.PlayModeTests` 11 项通过，控制台 0 错误。

### 阶段 15 内容扩展计划

#### 当前内容基线

- Luban 敌人表已有 6 条数据：3 个普通敌人、2 个精英敌人、1 个 Boss。
- Luban 升级选项表已有 19 条数据，当前生存局内只接入 Reward 基础属性效果。
- Luban 被动表已有 4 条数据，但生存局内阶段 8 的 3 个被动仍由代码侧定义效果。
- 运行时武器已有 2 种：追踪魔弹和旋刃。
- 固定地图、边界、吸附拾取、HUD、结算、对象池和轻量反馈已通过当前路线验收。
- 配置表内中文字段仍存在旧编码痕迹，用户可见文本暂由代码侧中文映射兜底。

#### 下一轮扩展优先级

1. **内容表清理与补表**：优先修复 Luban 表中文文本，补足武器表、被动效果表和刷怪阶段表，保持生成代码与 bytes 链路一致。
2. **敌人与刷怪扩展**：新增一批差异化普通敌人和精英敌人，并把刷怪权重、出现时间和精英/Boss 节点从代码阈值迁到配置。
3. **武器扩展**：新增 1 种低复杂度武器，例如近身脉冲或穿透飞镖；先复用现有投射物和舞台视图，不新增复杂进化系统。
4. **被动扩展**：把磁力、伤害、移速等被动效果迁入配置解释层，再补 1-2 个只影响现有数值的被动。
5. **资源与反馈扩展**：在玩法内容稳定后，再接入正式 Prefab、简单音效和命中特效，优先复用 TEngine 资源与音频模块。

#### 建议实施拆分

- 阶段 16：配置表清理与武器/被动/刷怪表结构补齐。
- 阶段 17：第一批敌人与刷怪节奏配置化。
- 阶段 18：第三武器最小实现与升级选项接入。
- 阶段 19：被动效果配置解释层与第一批扩展被动。
- 阶段 20：资源、音效和命中反馈最小接入。

#### 验收要求

- 每个扩展阶段必须保留现有核心闭环：开始、移动、刷怪、自动攻击、掉落、升级、结算和重开。
- Play Mode 测试应覆盖新增内容是否能被抽中、是否生效、是否与对象池和配置回退兼容。
- 不在内容扩展阶段引入技能树、稀有度、武器进化、复杂地图系统或第二套资源/配置框架。

## 后续推荐顺序

1. 阶段 1：玩家移动
2. 阶段 2：敌人生成与追踪
3. 阶段 3：生命值与受伤
4. 阶段 4：自动攻击武器
5. 阶段 5：经验、等级与三选一
6. 阶段 6：时间与难度曲线
7. 阶段 10：基础 UI 与反馈
8. 阶段 12：完整局内流程与结算
9. 阶段 7：多武器与武器升级
10. 阶段 8：被动道具与构筑
11. 阶段 9：对象池与性能优化
12. 阶段 13：Luban 配置化
13. 阶段 14：地图边界与拾取体验
14. 阶段 15：内容扩展计划
15. 阶段 16：配置表清理与表结构补齐
16. 阶段 17：敌人与刷怪节奏配置化扩展
17. 阶段 18：第三武器最小实现
18. 阶段 19：被动效果配置解释层
19. 阶段 20：资源、音效与命中反馈最小接入
20. 阶段 21：敌人与战斗内容第二轮扩展
21. 阶段 22：刷怪节奏与时间目标细化
22. 阶段 23：武器配置与数值平衡第一轮
23. 阶段 24：第四武器最小实现
24. 阶段 25：被动道具第二轮扩展
25. 阶段 26：掉落与金币用途最小闭环
26. 阶段 27：局内目标与胜利条件扩展
27. 阶段 28：Boss 战第一轮可玩化
28. 阶段 29：地图空间体验第一轮
29. 阶段 30：HUD 信息密度与可读性打磨
30. 阶段 31：升级三选一体验打磨
31. 阶段 32：结算与局外成长最小实现
32. 阶段 33：正式音效替换与混音规则
33. 阶段 34：命中特效与拾取特效 Prefab 接入
34. 阶段 35：敌人和投射物 Prefab 化第一轮
35. 阶段 36：配置校验与生成流程加固
36. 阶段 37：性能基线与 GC 观测
37. 阶段 38：对象池与大量敌人压测
38. 阶段 39：新手引导与操作提示最小实现
39. 阶段 40：版本验收与演示包准备
40. 阶段 41：伤害数字与暴击反馈表现
41. 阶段 42：视觉资产来源与许可清单
42. 阶段 43：奖励卡图标免费资源替换第一轮
43. 阶段 44：暴击特效分层与反馈强化
44. 阶段 45：Kenney 粒子贴图特效替换第一轮
45. 阶段 46：展示战斗构图模式第一轮
46. 阶段 47：展示截图 UI 控制第一轮
47. 阶段 48：HUD 构筑槽图标正式化第一轮
48. 阶段 49：伤害数字细节表现强化第二轮
49. 阶段 50：掉落物/拾取物正式表现第一轮
50. 阶段 51：升级奖励卡面正式底纹第一轮
51. 阶段 52：外部免费 UI 框体与提示装饰接入第一轮
52. 阶段 53：玩家与角色正式视觉表现第一轮
53. 阶段 54：敌人正式视觉表现第一轮
54. 阶段 55：命中、升级与拾取特效表现强化第三轮
55. 阶段 56：HUD 与提示动效反馈第一轮
56. 阶段 57：外部 UI 正式资产替换第二轮
57. 阶段 58：UI 槽位正式框体绑定
58. 阶段 59：UI 金币徽章正式框体绑定
59. 阶段 60：UI 提示面板正式装饰线绑定
60. 阶段 61：UI 奖励卡不可选遮罩正式框体绑定
61. 阶段 62：UI 奖励卡顶部徽带正式装饰绑定
62. 阶段 63：UI 奖励卡内层描边正式框体绑定
63. 阶段 64：RoguelikeGame 职责梳理与最小结构拆分
64. 阶段 65：奖励池构建职责拆分
65. 阶段 66：武器运行时解释职责拆分
