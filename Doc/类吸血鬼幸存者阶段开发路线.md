# 类吸血鬼幸存者阶段开发路线

## 项目目标

使用 Unity 和当前 TEngine 项目实现一个类《吸血鬼幸存者》的 2D 自动战斗肉鸽游戏。

核心体验：

- 玩家控制角色移动。
- 武器自动寻找并攻击敌人。
- 敌人从四周持续生成并追击玩家。
- 击杀敌人获得经验，升级时随机三选一。
- 随着时间推进形成武器、被动和数值构筑。

## 执行规则

1. 每次只推进一个阶段。
2. 当前阶段完成 Unity 运行验收后，再进入下一阶段。
3. 优先复用 TEngine 与当前项目已有模块。
4. 代码保持简洁，不为后续阶段提前建设复杂系统。
5. 对象池、Luban 配置化和内容扩展在核心闭环稳定后再推进。

## 当前阶段状态

| 阶段 | 状态 | 当前结论 |
| --- | --- | --- |
| 阶段 0：基础规则与约束 | 已完成 | 已建立根目录项目规范、框架优先和简洁代码规则 |
| 阶段 1：玩家移动 | 已完成 | `Rigidbody2D.MovePosition`、地图边界、暂停、重开归零和摄像机跟随均已通过 Play Mode 验收 |
| 阶段 2：敌人生成与追踪 | 已完成 | 已实现四周刷怪、逻辑追踪、`Rigidbody2D` 视图同步和触发碰撞体，并通过 Play Mode 验收 |
| 阶段 3：生命值与受伤 | 已完成 | 生命值、接触伤害间隔、死亡与结算链路已通过 Play Mode 验收 |
| 阶段 4：自动攻击武器 | 已完成 | 已实现最近敌人索敌、可见投射物、命中伤害和射程销毁，并通过 Play Mode 验收 |
| 阶段 5：经验、等级与三选一 | 已完成 | 经验升级、时间冻结、随机三选一和强化应用已通过 Play Mode 验收 |
| 阶段 6：时间与难度曲线 | 已完成 | 时间推进、刷怪数量和敌人生命、攻击、速度成长已通过 Play Mode 验收 |
| 阶段 7：多武器与武器升级 | 已完成 | 第二武器、武器解锁、武器升级和多武器同时执行已通过 Play Mode 测试验收 |
| 阶段 8：被动道具与构筑 | 已完成 | 已新增少量被动道具、被动汇总和 Play Mode 验收 |
| 阶段 9：对象池与性能优化 | 已完成 | 敌人、掉落物、投射物运行时对象已接入 TEngine `MemoryPool`，舞台临时视图已复用并通过 Play Mode 验收 |
| 阶段 10：基础 UI 与反馈 | 已完成 | HUD、升级三选一和结算 UI 已通过截图与 Play Mode 验收，按钮文字裁切已修复 |
| 阶段 11：音效、特效与打击感 | 已完成 | 已新增受击闪白、拾取脉冲和相机震动，正式音效待音频资源补齐后接入 TEngine AudioModule |
| 阶段 12：完整局内流程与结算 | 已完成 | 开始、游玩、升级、死亡、结算和重开流程已通过 Play Mode 验收 |
| 阶段 13：Luban 配置化 | 已完成 | 已复用现有 Luban 敌人表和升级选项表，完成最小配置化接入与 Play Mode 验收 |
| 阶段 14：地图边界与拾取体验 | 已完成 | 地图边界、掉落物吸附和拾取收集已通过 Play Mode 验收 |
| 阶段 15：内容扩展计划 | 已完成 | 已形成下一轮内容扩展优先级、配置边界和验收拆分 |
| 阶段 16：配置表清理与表结构补齐 | 已完成 | 已修复 Luban 表中文文本，补齐武器表、被动效果和刷怪阶段表，并完成生成链路验证 |
| 阶段 17：敌人与刷怪节奏配置化扩展 | 已完成 | 已新增刷怪阶段配置读取，普通敌人权重、精英/Boss 节点和生命成长从配置驱动 |
| 阶段 18：第三武器最小实现 | 已完成 | 已新增“穿透飞镖”，复用现有投射物、对象池和舞台视图，并接入升级池 |
| 阶段 19：被动效果配置解释层 | 已完成 | 已将磁力、投射物伤害和移动速度等被动效果接入配置解释层 |
| 阶段 20：资源、音效与命中反馈最小接入 | 已完成 | 已新增最小 WAV 音效资源，并在命中、拾取和升级节点通过 TEngine 音频模块安全触发 |
| 阶段 21：敌人与战斗内容第二轮扩展 | 已完成 | 已新增星尘游灵、蘑菇守卫、晶壳虫和月影骑士，并接入刷怪阶段配置与 Play Mode 测试 |
| 阶段 22：刷怪节奏与时间目标细化 | 已完成 | 已将 0-10 分钟刷怪节奏细化为 5 段，Boss 节点固定在 600 秒，并通过 Play Mode 测试覆盖阶段切换和流程稳定性 |
| 阶段 23：武器配置与数值平衡第一轮 | 已完成 | 已通过 `roguelike_weapon.xlsx` 微调追踪魔弹、旋刃和穿透飞镖，明确单体稳定、近身群体和远程穿透定位 |
| 阶段 24：第四武器最小实现 | 已完成 | 已新增低复杂度武器“星环脉冲”，复用现有投射物、舞台视图和对象池，不引入武器进化 |
| 阶段 25：被动道具第二轮扩展 | 已完成 | 已新增生息符文、鹰眼纹章和星辉轻靴，全部复用现有被动效果解释层与 HUD 汇总显示 |
| 阶段 26：掉落与金币用途最小闭环 | 已完成 | 已将局内金币接入升级三选一付费补给、余额不足阻止选择和结算入账闭环 |
| 阶段 27：局内目标与胜利条件扩展 | 已完成 | 已固化“击败地牢之心”作为最小胜利目标，并打通胜利结算与重开流程 |
| 阶段 28：Boss 战第一轮可玩化 | 已完成 | 已强化 Boss 出场提示、震屏反馈、体型/颜色表现和击败金币奖励，不新增复杂 Boss 技能系统 |
| 阶段 29：地图空间体验第一轮 | 已完成 | 已在固定地图中加入轻量障碍、简单阻挡和障碍表现，保持固定场地与简单碰撞 |
| 阶段 30：HUD 信息密度与二次元轻幻想表现打磨 | 已完成 | 已将局内 HUD 调整为低遮挡分区布局，补齐角色状态、时间压力、构筑槽和底部提示表现 |
| 阶段 31：升级三选一卡牌体验打磨 | 已完成 | 已将三选一升级打磨为二次元轻幻想卡牌界面，补齐图标、标题、描述、价格、不可选状态和选择反馈 |
| 阶段 32：结算与局外成长最小实现 | 已完成 | 已将永久金币成长展示为结算面板中的数据、收益和进度闭环，并接入下局初始攻击加成 |
| 阶段 33：正式音效替换与混音规则 | 已完成 | 已补齐 Boss/UI 音效资源，整理五类音效地址、音量和独立短冷却规则，继续走 TEngine AudioModule |
| 阶段 34：命中特效与拾取特效 Prefab 接入 | 已完成 | 已接入命中、击杀和拾取三类轻量 Prefab 特效，复用 TEngine 资源模块并保留运行时 Sprite 回退 |
| 阶段 35：敌人和投射物 Prefab 化第一轮 | 计划中 | 将关键敌人和投射物表现迁到 Prefab/资源地址，保留配置不可用回退 |
| 阶段 36：配置校验与生成流程加固 | 计划中 | 增加配置完整性校验、缺失资源提示和生成链路说明，减少表驱动风险 |
| 阶段 37：性能基线与 GC 观测 | 计划中 | 建立敌人数量、投射物数量、GC 和帧率的最小观测基线 |
| 阶段 38：对象池与大量敌人压测 | 计划中 | 在当前对象池基础上做高密度刷怪验证，并修正明显复用或释放问题 |
| 阶段 39：新手引导与操作提示最小实现 | 计划中 | 增加开局提示、移动说明、升级说明和结算提示，不新增复杂教程流程 |
| 阶段 40：版本验收与演示包准备 | 计划中 | 汇总阶段 21-39 的验收结果，准备一次可演示版本与后续路线复盘 |

## 当前推进边界

阶段 0-34 已完成，阶段 35-40 继续作为下一轮实施路线。当前下一阶段是阶段 35：敌人和投射物 Prefab 化第一轮。

后续推进必须继续遵守：

1. 第一批内容扩展优先补内容表和运行时最小解释逻辑，不改启动、热更或 UI 基础框架。
2. 每次只落地一个内容方向，例如敌人、武器、被动、刷怪节奏或资源表现中的一项。
3. 新增内容优先走 Luban 配置表；代码侧只保留运行时解释、发射模式和表现同步。
4. 每个实施小阶段仍需通过 `dotnet build`、`git diff --check` 和 Unity Play Mode 验收后再提交。

## 阶段 21-40 后续操作列表

### 阶段 21：敌人与战斗内容第二轮扩展

- 操作：在 `roguelike_enemy.xlsx` 中新增 2-3 个差异化普通敌人和 1 个精英敌人。
- 操作：补齐敌人生命、攻击、速度、权重和显示名，运行时继续复用 `RoguelikeContentCatalog` 与现有刷怪逻辑。
- 操作：新增 Play Mode 测试覆盖新增敌人可被配置读取、可进入刷怪池、核心战斗闭环不破。
- 实现记录：已新增普通敌人“星尘游灵”“蘑菇守卫”“晶壳虫”和精英敌人“月影骑士”。
- 实现记录：已将新增普通敌人接入阶段 1-3 普通刷怪池，并将“月影骑士”接入阶段 2-3 精英敌人配置。
- 验收：配置生成、`dotnet build`、`git diff --check`、Unity Play Mode 均通过，控制台 0 Error。

### 阶段 22：刷怪节奏与时间目标细化

- 操作：调整 `roguelike_spawn_stage.xlsx` 的 0-10 分钟阶段边界、普通怪数量、精英节点和 Boss 节点。
- 操作：补充运行时对阶段切换、Boss 节点和回退逻辑的测试。
- 操作：记录一轮 Play Mode 冒烟数据，包括存活时间、刷怪数量和 Boss 出现时间。
- 实现记录：刷怪阶段调整为 0 秒、60 秒、180 秒、360 秒、540 秒五段，普通刷怪数量从 1 逐步提升到 5。
- 实现记录：精英节点从 120 秒开始进入节奏，后期 540 秒持续承压；Boss 节点固定在 600 秒。
- 实现记录：运行时 Boss 节点改为到点后只生成一次，避免 10 分钟后重复刷 Boss。
- 验收：配置生成、`dotnet build`、`git diff --check`、Unity Play Mode 均通过，阶段切换稳定，新增节奏不破坏开始、升级、死亡、结算和重开。

### 阶段 23：武器配置与数值平衡第一轮

- 操作：复核追踪魔弹、旋刃、穿透飞镖的基础伤害、冷却、射程和升级收益。
- 操作：优先通过 `roguelike_weapon.xlsx` 调整数值，代码侧只保留解释逻辑。
- 操作：新增或扩展测试覆盖三把武器升级后输出差异。
- 实现记录：追踪魔弹调整为稳定单体输出，基础伤害 13、基础间隔 0.52、射程 5.8。
- 实现记录：旋刃调整为近身群体输出，基础伤害 8、基础间隔 0.78、射程 3.0。
- 实现记录：穿透飞镖调整为远程穿透输出，基础伤害 11、伤害成长 4、基础间隔 0.82、射程 7.8。
- 验收：三把武器均可被抽中、升级并在同局内同时生效，配置生成、`dotnet build`、`git diff --check`、Unity Play Mode 均通过。

### 阶段 24：第四武器最小实现

- 操作：新增 1 把低复杂度武器，优先选择近身脉冲或弧形散射等复用现有表现的方案。
- 操作：接入武器配置、升级池、HUD 汇总和舞台视图颜色/形状。
- 操作：补充武器解锁、升级和发射行为测试。
- 实现记录：新增“星环脉冲”，定位为短程环形脉冲武器，按冷却向周围释放多道短程投射物。
- 实现记录：星环脉冲接入 `roguelike_weapon.xlsx`、升级三选一、武器汇总、投射物颜色/形状和对象池复用链路。
- 验收：不引入武器进化、稀有度或第二套投射物框架，配置生成、`dotnet build`、`git diff --check`、Unity Play Mode 均通过。

### 阶段 25：被动道具第二轮扩展

- 操作：新增 2-3 个只影响现有数值的被动，例如冷却缩减、经验获取、最大生命。
- 操作：扩展 `EEffectType` 和解释层时保持最小范围，保留配置不可用回退。
- 操作：补充被动重复获得、HUD 显示和属性生效测试。
- 实现记录：新增“生息符文”“鹰眼纹章”“星辉轻靴”三个被动道具，分别影响最大生命与恢复、攻击与暴击、移动速度与拾取吸附范围。
- 实现记录：本阶段未新增冷却缩减或经验获取效果，优先复用现有 `AddMaxHealth`、`Heal`、`AddAttack`、`AddCritChance`、`AddMoveSpeed` 和 `AddPickupRadius`，避免提前扩展武器冷却解释逻辑。
- 实现记录：新增 `RoguelikeStage25PassiveExpansionTests`，覆盖 Luban bytes 读取、被动选择池、属性生效、重复叠加和 HUD 汇总显示。
- 验收：配置生成、`dotnet build`、`git diff --check`、Unity Play Mode 39/39 均通过，控制台 0 Error。
- 验收：被动选择不依赖 UI 脚本硬编码规则。

### 阶段 26：掉落与金币用途最小闭环

- 操作：明确金币在局内商店、结算或局外成长中的一个最小用途。
- 操作：补齐金币消耗、余额显示和失败提示。
- 操作：新增测试覆盖金币获得、消费和余额不足分支。
- 实现记录：金币掉落继续由敌人击杀概率产出，拾取后进入 `CurrentRun.Gold`，可作为局内升级三选一的消费货币。
- 实现记录：`ChooseReward` 现在会检查 `Cost/CanAfford`，金币不足时保留 `RewardChoice` 并显示失败提示，金币足够时先扣费再应用效果。
- 实现记录：新增付费选项“战地补给”，花费 8 金币立即恢复 35 生命，形成最小局内消费闭环。
- 实现记录：结算仍将未消费的局内金币一次性入账永久金币，`_metaSaved` 防止重复入账。
- 验收：新增 `RoguelikeStage26GoldLoopTests` 覆盖金币拾取、消费、余额不足和结算入账；`dotnet build`、`git diff --check`、Unity Play Mode 43/43 均通过，控制台 0 Error。
- 验收：金币不再只是收集数字，且不引入复杂经济系统。

### 阶段 27：局内目标与胜利条件扩展

- 操作：固化一条最小胜利路线，例如存活到指定时间或击败 Boss。
- 操作：补齐 `Victory` 结算文案、UI 显示和重开流程。
- 操作：新增测试覆盖胜利、失败和重开后状态归零。
- 实现记录：运行时敌人新增配置 ID 与 Boss 标记，配置表 Boss 敌人生成后会保留身份，用于判断胜利目标。
- 实现记录：Boss 被击败时进入 `Victory`，停止当帧后续战斗更新，显示“击败地牢之心，生存目标达成。”并将局内金币结算入账。
- 实现记录：普通敌人击杀仍按原逻辑生成经验/金币掉落，不触发胜利。
- 实现记录：新增 `RoguelikeStage27VictoryConditionTests`，覆盖 Boss 击杀胜利、普通击杀不胜利和胜利后重开归零。
- 验收：`dotnet build`、`git diff --check`、Unity Play Mode 46/46 均通过，控制台 0 Error。
- 验收：单局有明确结束目标，而不是只依赖死亡结束。

### 阶段 28：Boss 战第一轮可玩化

- 操作：强化 Boss 基础数值、体型/颜色表现、出现提示和击败奖励。
- 操作：只做一个 Boss 节奏，不新增复杂技能树或多阶段 Boss 框架。
- 操作：补充 Boss 出现、受击、击败和结算测试。
- 实现记录：Boss 生成时写入“地牢之心出现”提示并触发更强相机震动，让 600 秒 Boss 节点可被玩家明确感知。
- 实现记录：舞台视图会根据 `IsBoss` 将 Boss 显示为更大的紫色敌人，普通敌人仍保留原红色表现和受击闪白。
- 实现记录：击败 Boss 后额外获得 25 金币，再进入 `Victory` 并执行一次性结算入账。
- 实现记录：新增 `RoguelikeStage28BossPlayableTests`，覆盖 Boss 出场身份与提示、Boss 视图表现差异、Boss 击败奖励与胜利结算。
- 验收：`dotnet build`、`git diff --check`、Unity Play Mode 49/49 均通过，控制台 0 Error。
- 验收：Boss 战能被玩家明显感知，且不破坏普通刷怪节奏。

### 阶段 29：地图空间体验第一轮

- 操作：调优地图尺寸、边界表现或加入少量简单障碍。
- 操作：障碍仅使用简单碰撞和当前移动逻辑，不引入复杂地图生成。
- 操作：补充玩家边界、敌人追踪和投射物行为测试。
- 实现记录：运行时新增 3 个固定 `RoguelikeArenaObstacle`，用于形成轻量绕行空间，不新增随机地图或寻路系统。
- 实现记录：玩家同步位置与运行时移动会被推出障碍，敌人追踪同样受障碍阻挡，投射物命中障碍后回收。
- 实现记录：舞台视图绘制障碍块与高光，让固定地图从纯地砖变为有空间层次的场地。
- 实现记录：新增 `RoguelikeStage29MapSpaceTests`，覆盖玩家/敌人障碍阻挡、投射物障碍回收和障碍视图绘制。
- 验收：`dotnet build`、`git diff --check`、Unity Play Mode 52/52 均通过，控制台 0 Error。
- 验收：地图空间更有变化，但仍保持固定场地。

### 阶段 30：HUD 信息密度与二次元轻幻想表现打磨

- 操作：整理生命、经验、等级、时间、击杀、武器和被动的显示优先级。
- 操作：以“二次元轻幻想 Bullet Heaven UI”为目标，采用左上角色头像、生命/经验/等级，中上生存时间与敌人压力，右上武器/被动图标槽，底部低遮挡操作提示的结构。
- 操作：优先使用免费可商用字体、基础 UI 资源和 GPT 绘画产出的头像、图标或占位资源，所有外部资源必须记录来源、许可和用途。
- 操作：修复可能的文字裁切、层级遮挡和状态延迟刷新。
- 操作：保留 `GameModule.UI.ShowUIAsync<T>()` 与当前 UIWindow 体系。
- 实现记录：`BattleHudUI` 从顶部整条信息栏调整为左上角色状态面板、中上时间压力面板、右上构筑槽面板和底部低遮挡提示面板。
- 实现记录：角色状态面板展示头像占位、生命条、经验条、等级、生命、攻击、局内金币和永久金币；构筑面板展示武器/被动图标槽与构筑摘要。
- 实现记录：新增 `RoguelikeStage30HudPolishTests`，覆盖 HUD 分区锚点、角色/压力/构筑文本刷新和武器/被动图标槽生成。
- 验收：`dotnet build`、`git diff --check`、Unity Play Mode 55/55 均通过，最终控制台 0 Error。

### 阶段 31：升级三选一卡牌体验打磨

- 操作：优化升级选项池权重、重复出现规则和已满级武器过滤。
- 操作：将升级选择从基础按钮升级为卡牌式奖励界面，每张卡包含图标、标题、效果描述、价格或条件、可选和不可选状态。
- 操作：增加选择反馈、价格提示、金币不足或不可选状态提示。
- 操作：卡牌视觉优先体现二次元轻幻想风格，适合 Steam 截图展示，但不提前引入复杂稀有度、词条或武器进化系统。
- 操作：补充选项池生成和选择后状态恢复测试。
- 实现记录：`BattleChoiceUI` 从基础按钮升级为三张奖励卡，包含图标占位、标题、描述、价格和状态提示。
- 实现记录：金币不足时付费卡显示“金币不足”并不可点击，金币足够时点击后继续复用 `RoguelikeGame.ChooseReward` 恢复战斗。
- 实现记录：`RoguelikeSurvivalWeapon` 增加当前阶段 5 级上限，满级武器升级项不再进入奖励池。
- 实现记录：新增 `RoguelikeStage31RewardCardTests`，覆盖卡牌结构、价格/不可选状态、选择后恢复运行和满级武器过滤。
- 验收：未引入稀有度、词条或复杂构筑系统；`dotnet build`、`git diff --check`、Unity Play Mode 59/59 均通过，最终控制台 0 Error。

### 阶段 32：结算与局外成长最小实现

- 操作：将单局金币或成绩接入一个最小局外成长项。
- 操作：使用现有存档方式保存简单数值，避免新增账号、背包或复杂存档系统。
- 操作：结算界面区分胜利/失败标题、数据区、收益区、永久成长进度和重新开始入口，允许预留角色立绘或剪影区域。
- 操作：补充结算入账、重开保留和异常回退测试。
- 实现记录：继续使用现有永久金币 `PlayerPrefs` 存档；每 100 永久金币提供下局初始攻击 +1。
- 实现记录：结算界面已拆分为角色剪影、胜败标题、战斗数据、收益区、永久成长进度条、成长说明和重新开始入口。
- 实现记录：新增 `RoguelikeStage32SettlementGrowthTests` 覆盖入账一次、重开加成和结算 UI 展示。
- 验收：`dotnet build`、`git diff --check` 和 Unity Play Mode `GameLogic.PlayModeTests` 62/62 通过，最终控制台 0 Error。

### 阶段 33：正式音效替换与混音规则

- 操作：替换当前占位 WAV，整理命中、拾取、升级、Boss 和 UI 音效地址。
- 操作：通过 TEngine `IAudioModule` 播放，补齐音量和短冷却规则。
- 操作：补充资源存在性和安全播放测试。
- 实现记录：新增原创短音效 `Roguelike_Boss.wav` 和 `Roguelike_UIConfirm.wav`，当前五类音效均有资源地址。
- 实现记录：音效路径、音量和冷却整理为 `RoguelikeGame` 常量；运行时仍通过 `GameModule.Audio.Play(AudioType.Sound, ...)` 播放。
- 实现记录：音效冷却从全局单冷却改为按音效地址独立冷却，避免命中、拾取、升级、Boss 和 UI 确认音互相阻塞。
- 实现记录：新增 `RoguelikeStage33AudioMixTests` 覆盖资源存在、音量/冷却范围、UI 确认音和 Boss 音效互不阻塞。
- 验收：`dotnet build`、`git diff --check` 和 Unity Play Mode `GameLogic.PlayModeTests` 66/66 通过，最终控制台 0 Error。

### 阶段 34：命中特效与拾取特效 Prefab 接入

- 操作：为命中、击杀、拾取至少接入一组轻量 Prefab 表现。
- 操作：复用 TEngine 资源模块和当前舞台视图生命周期。
- 操作：补充特效生成、回收和配置缺失回退测试。
- 验收：不新增第二套特效框架。
- 实现记录：新增 `Roguelike_HitEffect.prefab`、`Roguelike_KillEffect.prefab`、`Roguelike_PickupEffect.prefab` 和自制白色像素 Sprite，均放入 `Assets/AssetRaw/Effects`，继续使用现有 `AddressByFileName` 资源收集规则。
- 实现记录：`RoguelikeGame` 新增命中、击杀、拾取三类 `RoguelikeEffectCue`，由真实战斗事件驱动表现层，不让舞台视图猜测对象删除原因。
- 实现记录：`RoguelikeBattleStageView` 增加 Prefab 优先、Sprite 回退的轻量特效池；资源模块不可用或地址缺失时不影响战斗流程。
- 实现记录：新增 `RoguelikeStage34EffectPrefabTests`，覆盖资源存在、三类 cue 生成、舞台特效生成/回收和缺失资源回退。
- 验收：`dotnet build UnityProject\UnityProject.sln --no-restore`、`git diff --check` 和 Unity Play Mode `GameLogic.PlayModeTests` 69/69 均通过，最终控制台 0 Error。

### 阶段 35：敌人和投射物 Prefab 化第一轮

- 操作：选择 1-2 个敌人和 1-2 个投射物迁移到正式 Prefab 表现。
- 操作：资源地址优先从配置或统一映射读取，保留运行时 Sprite 回退。
- 操作：补充资源加载失败、对象池复用和表现同步测试。
- 验收：不改启动、热更和资源基础流程。

### 阶段 36：配置校验与生成流程加固

- 操作：为敌人、武器、被动、刷怪阶段增加最小配置完整性校验。
- 操作：补充缺失 ID、重复 ID、空奖励池和缺失资源地址的错误提示。
- 操作：更新 Luban 生成与验证说明到现有有效文档。
- 验收：配置错误能尽早暴露，不靠运行时空引用暴露。

### 阶段 37：性能基线与 GC 观测

- 操作：建立一套敌人数量、投射物数量、对象池数量、GC 和帧率的观测口径。
- 操作：优先使用 Unity Profiler、MCP 可读数据或轻量运行时统计。
- 操作：记录基线数据，不先做大规模优化。
- 验收：形成可复测的性能基线和后续优化入口。

### 阶段 38：对象池与大量敌人压测

- 操作：在当前对象池基础上提高刷怪密度进行压测。
- 操作：检查敌人、掉落物、投射物和临时视图的归还与复用。
- 操作：补充高密度情况下的 Play Mode 测试或冒烟验证。
- 验收：无明显对象泄漏、重复释放或视图残留。

### 阶段 39：新手引导与操作提示最小实现

- 操作：增加开局移动提示、自动攻击说明、升级说明和结算提示。
- 操作：复用当前 UI 体系，优先做轻量提示，不新增复杂教程状态机。
- 操作：补充提示显示、隐藏和重开重置测试。
- 验收：第一次游玩能理解核心操作和目标。

### 阶段 40：版本验收与演示包准备

- 操作：汇总阶段 21-39 的功能、测试、已知问题和演示路径。
- 操作：执行完整构建、Play Mode、控制台和手动冒烟验收。
- 操作：必要时整理一份中文演示说明，收敛到现有文档索引。
- 操作：准备至少 3 张可用于 Steam 商店页讨论的截图视角：高密度战斗、升级奖励三选一、结算成长界面。
- 验收：截图中体现“二次元轻幻想 Bullet Heaven UI”：角色感、卡牌奖励、图标化构筑、低遮挡 HUD。
- 验收：避免 UI 呈现廉价手游感，包括满屏按钮、过度红点、礼包式入口和遮挡战斗主体的大面板。
- 验收：形成一个可演示版本，并明确下一轮阶段边界。

### 阶段 1 实现记录

- 玩家运行时视图挂载 `Rigidbody2D`、`CircleCollider2D` 和轻量移动组件。
- 移动在 `FixedUpdate` 中通过 `Rigidbody2D.MovePosition` 执行。
- `RoguelikeGame` 继续统一保存输入、速度、地图边界和同步后的玩家位置。
- 主摄像机加入平滑跟随，并处理运行时舞台重建时的摄像机保留。
- Unity Play Mode 验收结果：移动、暂停、边界、重开归零、摄像机跟随均通过，控制台 0 错误。

### 阶段 2-3 实现记录

- 敌人运行时视图已挂载运动学 `Rigidbody2D`、触发碰撞体和物理帧同步组件。
- 保留现有四周生成、随时间增量和逻辑追踪规则。
- Unity Play Mode 验收结果：敌人持续生成并追近玩家，接触伤害生效，控制台 0 错误。

### 阶段 4 实现记录

- 原方向范围攻击已替换为最近敌人自动索敌和直线投射物。
- 投射物具备速度、伤害、剩余射程和独立运行时视图。
- Unity Play Mode 验收结果：投射物生成、命中、伤害和击杀均通过，控制台 0 错误。

### 阶段 5-6 实现记录

- 升级时进入三选一阶段并冻结局内时间，选择后恢复运行并应用属性强化。
- 时间推进会增加单次刷怪数，并提高敌人生命、攻击和速度。
- Unity Play Mode 验收结果：升级与难度曲线均通过，控制台 0 错误。

### 阶段 10/12 实现记录

- HUD、升级三选一和结算界面已完成截图复验。
- 修复公共按钮标签高度不足导致小按钮文字被完全裁切的问题。
- 开始、升级、死亡、结算和重开流程均已通过 Play Mode 验收，控制台 0 错误。

### 阶段 11 实现记录

- 复用当前运行时生成的轻量 Sprite 表现，不新增第二套特效框架。
- 敌人受击继续保留短时闪白，投射物命中和敌人击杀会触发短时相机震动。
- 经验或金币拾取会触发 `PickupFlash`，舞台视图在玩家位置显示短时拾取脉冲。
- 当前仓库 `Assets/AssetRaw/Audios` 暂无可用音频资源，因此未硬接空路径音效；后续正式音效应通过 TEngine `IAudioModule.Play(AudioType.Sound, path)` 接入资源链路。
- 已新增 `RoguelikeStage11FeedbackTests`，覆盖命中反馈、拾取反馈、舞台反馈视图和反馈计时衰减。
- Unity Play Mode 验收结果：`GameLogic.PlayModeTests` 14 项通过，控制台 0 错误。

### 阶段 7 实现记录

- 新增运行时武器状态，默认武器为“追踪魔弹”。
- 升级三选一加入“魔弹升级”“解锁旋刃”和“旋刃升级”。
- “旋刃”为第二种差异化武器，会按冷却向周围环形齐射短程刀刃。
- HUD 会显示当前武器等级，投射物按武器来源使用不同视觉颜色。
- 已新增 `GameLogic.PlayModeTests`，覆盖武器选项解锁、武器升级和多武器同时发射。
- Unity Play Mode 验收结果：`RoguelikeStage7WeaponTests` 2 项通过，控制台 0 错误。

### 阶段 8 实现记录

- 升级三选一新增“磁力核心”“聚能护符”“疾风靴”三种被动道具。
- 被动道具复用 `RoguelikeRelicTemplate` 和 `CurrentRun.Relics`，仅影响拾取吸附、投射物伤害和移动速度。
- HUD 新增被动汇总显示，重复获得同一被动时显示叠加数量。
- Unity Play Mode 验收结果：`GameLogic.PlayModeTests` 4 项通过，控制台 0 错误。

### 阶段 9 实现记录

- 敌人、掉落物和投射物实现 `IMemory`，由 TEngine `MemoryPool` 获取和释放。
- 重开、敌人死亡、投射物命中或耗尽、掉落物拾取时会归还池化运行时对象。
- 舞台视图新增敌人、掉落物和投射物 Transform 复用栈，实体消失后不再直接销毁临时视图。
- Unity 组件获取改为显式 Unity 判空，避免池化视图复用时受到 `UnityEngine.Object` 假 null 影响。
- Unity Play Mode 验收结果：`GameLogic.PlayModeTests` 6 项通过，控制台 0 错误。

### 阶段 13 实现记录

- 复用现有 Luban 生成配置：`TbRoguelikeEnemy`、`TbRoguelikeChoice` 和 `TbRoguelikeRelic`。
- 生存刷怪优先从敌人配置表读取基础生命和攻击，并按波次继续叠加运行时难度成长。
- 升级三选一会从配置表读取基础奖励效果，配置不可用时回退到原有硬编码基础选项。
- 当前表结构尚未覆盖速度、范围、攻击频率、武器和阶段 8 被动效果，这些仍保留代码定义，避免阶段 13 扩表过重。
- 已新增 `RoguelikeStage13ConfigTests`，覆盖配置二进制加载、配置选项效果应用和配置敌人刷怪。
- Unity Play Mode 验收结果：`GameLogic.PlayModeTests` 9 项通过，控制台 0 错误。

### 阶段 14 实现记录

- 复核现有地图边界、掉落物吸附和拾取逻辑，当前实现满足阶段 14 最小目标。
- 新增 `RoguelikeStage14BoundaryPickupTests`，覆盖玩家位置边界夹取、近距离经验掉落吸附和收集入账。
- 未新增地图系统、掉落表或复杂拾取反馈，避免越过阶段 14 边界。
- Unity Play Mode 验收结果：`GameLogic.PlayModeTests` 11 项通过，控制台 0 错误。

### 阶段 15 内容扩展计划

#### 当前内容基线

- Luban 敌人表已有 6 条数据：3 个普通敌人、2 个精英敌人、1 个 Boss。
- Luban 升级选项表已有 19 条数据，当前生存局内只接入 Reward 基础属性效果。
- Luban 被动表已有 4 条数据，但生存局内阶段 8 的 3 个被动仍由代码侧定义效果。
- 运行时武器已有 2 种：追踪魔弹和旋刃。
- 固定地图、边界、吸附拾取、HUD、结算、对象池和轻量反馈已通过当前路线验收。
- 配置表内中文字段仍存在旧编码痕迹，用户可见文本暂由代码侧中文映射兜底。

#### 下一轮扩展优先级

1. **内容表清理与补表**：优先修复 Luban 表中文文本，补足武器表、被动效果表和刷怪阶段表，保持生成代码与 bytes 链路一致。
2. **敌人与刷怪扩展**：新增一批差异化普通敌人和精英敌人，并把刷怪权重、出现时间和精英/Boss 节点从代码阈值迁到配置。
3. **武器扩展**：新增 1 种低复杂度武器，例如近身脉冲或穿透飞镖；先复用现有投射物和舞台视图，不新增复杂进化系统。
4. **被动扩展**：把磁力、伤害、移速等被动效果迁入配置解释层，再补 1-2 个只影响现有数值的被动。
5. **资源与反馈扩展**：在玩法内容稳定后，再接入正式 Prefab、简单音效和命中特效，优先复用 TEngine 资源与音频模块。

#### 建议实施拆分

- 阶段 16：配置表清理与武器/被动/刷怪表结构补齐。
- 阶段 17：第一批敌人与刷怪节奏配置化。
- 阶段 18：第三武器最小实现与升级选项接入。
- 阶段 19：被动效果配置解释层与第一批扩展被动。
- 阶段 20：资源、音效和命中反馈最小接入。

#### 验收要求

- 每个扩展阶段必须保留现有核心闭环：开始、移动、刷怪、自动攻击、掉落、升级、结算和重开。
- Play Mode 测试应覆盖新增内容是否能被抽中、是否生效、是否与对象池和配置回退兼容。
- 不在内容扩展阶段引入技能树、稀有度、武器进化、复杂地图系统或第二套资源/配置框架。

## 后续推荐顺序

1. 阶段 1：玩家移动
2. 阶段 2：敌人生成与追踪
3. 阶段 3：生命值与受伤
4. 阶段 4：自动攻击武器
5. 阶段 5：经验、等级与三选一
6. 阶段 6：时间与难度曲线
7. 阶段 10：基础 UI 与反馈
8. 阶段 12：完整局内流程与结算
9. 阶段 7：多武器与武器升级
10. 阶段 8：被动道具与构筑
11. 阶段 9：对象池与性能优化
12. 阶段 13：Luban 配置化
13. 阶段 14：地图边界与拾取体验
14. 阶段 15：内容扩展计划
15. 阶段 16：配置表清理与表结构补齐
16. 阶段 17：敌人与刷怪节奏配置化扩展
17. 阶段 18：第三武器最小实现
18. 阶段 19：被动效果配置解释层
19. 阶段 20：资源、音效与命中反馈最小接入
20. 阶段 21：敌人与战斗内容第二轮扩展
21. 阶段 22：刷怪节奏与时间目标细化
22. 阶段 23：武器配置与数值平衡第一轮
23. 阶段 24：第四武器最小实现
24. 阶段 25：被动道具第二轮扩展
25. 阶段 26：掉落与金币用途最小闭环
26. 阶段 27：局内目标与胜利条件扩展
27. 阶段 28：Boss 战第一轮可玩化
28. 阶段 29：地图空间体验第一轮
29. 阶段 30：HUD 信息密度与可读性打磨
30. 阶段 31：升级三选一体验打磨
31. 阶段 32：结算与局外成长最小实现
32. 阶段 33：正式音效替换与混音规则
33. 阶段 34：命中特效与拾取特效 Prefab 接入
34. 阶段 35：敌人和投射物 Prefab 化第一轮
35. 阶段 36：配置校验与生成流程加固
36. 阶段 37：性能基线与 GC 观测
37. 阶段 38：对象池与大量敌人压测
38. 阶段 39：新手引导与操作提示最小实现
39. 阶段 40：版本验收与演示包准备
