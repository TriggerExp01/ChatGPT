# 项目开发规范

## 框架优先原则

1. 实现功能前，必须先检查 TEngine、当前项目业务模块和现有工具中是否已经提供可复用的方案、接口或实现模式。
2. 能使用框架已有能力完成的功能，优先使用框架方案，不重复实现同类基础设施。
3. 优先复用以下现有能力：
   - 通过 `GameModule` 访问框架模块。
   - 使用框架 UI 窗口、层级和生命周期管理界面。
   - 使用流程系统管理启动、切换和阶段流程。
   - 使用事件系统处理跨模块通知。
   - 使用 YooAsset 和框架资源模块加载资源。
   - 使用 Luban 配置系统管理内容数据。
   - 使用框架计时器、对象池、内存池、音频、本地化和调试工具。
4. 新增自定义方案前，应先搜索：
   - `Doc/框架使用指南.md`
   - `Books/`
   - `UnityProject/repowiki/zh/content/`
   - `UnityProject/Assets/TEngine/`
   - 当前项目已有业务代码
5. 框架现有方案无法满足需求时，可以扩展或新增实现，但需要：
   - 说明现有方案不适用的原因。
   - 保持与框架生命周期、模块边界和命名风格一致。
   - 避免绕过框架形成第二套资源、UI、配置、事件或流程体系。

## 通用规则

1. 用户可见文案、项目文档名称和文档内容使用中文。
2. 重要开发结论写入 `Doc`。
3. 代码风格讲究简洁、高效、易读，优先使用最直接且符合现有架构的实现。
4. 避免过度设计：
   - 不为尚未确认的未来需求提前增加抽象层、接口、基类或复杂扩展机制。
   - 不把简单逻辑拆成大量零散类和文件。
   - 不引入无法降低当前复杂度的设计模式。
   - 不进行与当前需求无关的重构。
5. 只有在能够消除实际重复、降低复杂度、明确改善性能或符合框架既有模式时，才增加抽象或拆分模块。
6. 性能优化应针对明确瓶颈；没有测量依据时，优先保证代码清晰和正确。
7. 优先保证功能可运行，再进行必要的结构收口。
8. 修改后至少执行脚本编译和代码格式检查。

## 阶段化开发规则

1. 当前产品目标是类《吸血鬼幸存者》的 2D 自动战斗肉鸽游戏。
2. 开发按 [类吸血鬼幸存者阶段开发路线.md](D:/Work/Unity_Project/TEngine_Game/Doc/类吸血鬼幸存者阶段开发路线.md:1) 分阶段推进。
3. 每次只推进当前阶段；当前阶段未完成运行验收前，不跨阶段堆叠新系统。
4. 修改前先分析已有脚本，优先复用现有系统，不创建职责重叠的脚本。
5. 每完成一个阶段，必须记录：
   - 完成内容
   - 修改文件
   - Unity 验证步骤
   - 实际验证结果
6. 如果当前架构阻碍下一阶段，先记录问题和最小调整方案，不直接进行大规模重构。
